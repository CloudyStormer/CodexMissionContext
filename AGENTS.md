# CodexMissionContext 仓库维护规则

## 新克隆自动接入

当仓库根目录不存在 `.device-id` 时，将其视为设备 B 的首次克隆。不要要求用户重新解释同步目标，直接执行：

1. 阅读 `README.md`、`同步清单/项目清单.md`、`同步清单/任务清单.md`、`同步清单/侧边栏布局.md`、`同步清单/增量对齐规则.md` 和 `设备/B/首次接入指令.md`。
2. 运行 `./脚本/安装到本机.sh B`。
3. 读取本机 Codex 项目与任务列表：把清单外可同步项目及其 Git 来源发布到项目并集，再补充本机缺失的并集项目和清单长期任务；随后按最新侧边栏布局对齐共享项目顺序、项目置顶和共同长期任务置顶。绝不删除、归档、重命名或隐藏任何现有项。
4. 把设备 B 的本地 `projectId`、`threadId` 和已应用版本写入 `设备/B/`。
5. 运行仓库检查，提交并推送首次接入结果。
6. 如果当前 Codex 表面不具备项目或任务管理能力，完成其余可执行步骤并准确报告唯一缺口，不得假装完成。

`AI学习`、`AI 教学助手`、`AI 笔记助手` 是设备 A 的个人内容，永久排除在双设备共同范围之外。不要读取其上下文、不要在设备 B 创建、不要把它们当作阻塞项；除非用户以后明确改变范围。

## 日常维护

- 所有面向用户的说明、项目记录和任务记录使用中文；标准技术标识、命令和文件名可以保留英文。
- 设备 A、B 只是本地标识，没有主设备、次设备或固定接力顺序。用户会交替使用两台设备；任一设备都必须在开始时拉取、接续最新记录，并在结束时及时更新和推送。
- Windows 设备上的所有双设备共同项目必须位于 `D:\.aaProject-Bruce\` 下，禁止放到 C 盘；共同任务直接使用 D 盘已保存项目，不在 C 盘创建 Codex worktree 或项目副本。系统程序和不含项目内容的 Codex 自身状态目录不属于项目存放范围。
- 无论项目内容多少，同步对象始终是保留完整语义的精准压缩总结，不是原始对话、全量项目内容或代码副本。
- 修改任何内容前，先阅读 `README.md`、`项目上下文/项目索引.md` 和相关模板。
- 不把项目代码复制进本仓库。项目代码继续由它自己的 Git 仓库管理。
- 每项需要长期续接的任务使用独立记录；共同项目中的临时对话不自动进入记录。
- 记录完整语义：用户要求、结论、决定原因、文件、命令、测试、失败、边界、风险、遗留问题和下一步。
- 不保存原始聊天全文，不保存密码、令牌、Cookie、私钥、账号缓存、本地数据库或 session 文件。
- 不覆盖另一台设备的未合并记录。任务开始先同步；任务结束先检查，再提交和推送。
- 对齐项目和任务列表时遵循 `同步清单/增量对齐规则.md`。项目采用 A、B 可同步项目的并集，只添加缺失项，绝不删除、归档、重命名或隐藏现有项；任务仍只同步清单规定的长期任务。共享项目顺序、项目置顶和共同长期任务置顶按 `同步清单/侧边栏布局.md` 的版本协议对齐。
- 应用共享侧边栏布局时只重排共享对象子序列，保留个人/临时对象的置顶状态和相对顺序；只能使用正式 API 或用户可见界面，不能保证时标记部分应用，禁止直接写 Codex 原始状态或数据库。
- 最近临时对话不进入共同任务清单；只有 `同步清单/任务清单.md` 中的长期任务需要在两台设备共同存在。
- 用户发送“开始同步”时，仍须把所有可见共同项目临时对话的完整语义写入同步报告；这不等于在另一端创建临时任务。
- 共同项目不等于其中所有对话都是共同任务；新任务只有经用户明确指定为长期任务，才加入清单。
- 推送失败、冲突或检查失败时，明确报告实际状态，不得声称已经同步。
- 普通任务只提交本任务涉及的文件，保留用户或另一设备的无关修改；四字口令“开始同步”和项目提交命令属于明确例外，按 `check-projects` Skill 审查并处理检查范围内已有的普通改动。
- 用户以命令方式发送“检查提交”“检查项目”“项目检查”或“项目提交”时，调用 `check-projects` Skill；该明确授权允许在审查和验证后处理项目列表内已有的普通改动。用户单独发送“开始”时，用同一 Skill 核对并补齐两端项目并集。用户发送四字口令“开始同步”时，以同一运行 ID 尝试让 A、B 各由一个维护任务走查本机所有共同项目、全部 Git 仓库和所有可见共同项目对话：代码先取得并安全对齐远端，再审查、验证、提交和推送安全的本地普通改动，最后合并两端交接；对端不可达时不得声称双端完成。
