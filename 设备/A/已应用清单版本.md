# 设备 A 已应用清单版本

- 项目清单版本：4
- 目标项目清单版本：9
- 任务清单版本：4
- 目标任务清单版本：5
- 完全对齐交接包版本：协议 `1`、目标修订 `1`；运行 `A-20260802T200017+0800` 已生成 A 任务快照，因 B 维护任务不可达且缺 B 快照，未生成合并包或 A/B 回执
- 设备状态确认来源与时间：设备 A 正式项目/任务列表与逐仓复核，2026-08-02 20:17（Asia/Shanghai）
- 已完整应用侧边栏布局版本：0
- 已部分应用侧边栏布局版本：2
- 本设备已确认待应用侧边栏布局版本：无
- 已部分应用未完成逻辑 ID：版本 `2` 的 `demo`
- 本机上次确认布局基线：版本 `2`；快照 `同步清单/侧边栏布局候选/1-A-20260731T180051Z-vid-pin.md`；规范化 SHA-256 `9572bd1210a264d270ddadee924bfb626efee445e663413fd9f008a7c077ad12`；比较时过滤 `demo`
- 项目清单未完成逻辑 ID：`demo`
- 应用日期：2026-08-02
- 状态：部分应用；任务清单版本 `5` 的 39 项目标在 A 端已有 38 项真实实体，仅 `demo-foreman` 因 Demo 无 HEAD 且项目不存在而缺失；还没有双端同摘要、有效完整任务顺序和双端回执
- 策略：只增不减
- 已完成项：`CodexMissionContext`、`AI工作流（试点）`、`lottery`、`vid-mat-lab`、`Aidrama`、`canvas-garment`、`funhub-space` 和三个 liquidity 项目已出现在设备 A 左侧并回写真实 `projectId`。A 端已把 21 个 B-only 任务补成镜像，回读任务清单版本 `5` 的 38 个现存实体全部可见、标题精确、非置顶，`unavailableHosts=[]`、`unavailableSources=[]`。AIWorkflow 已拉取到 `25d7ff5`。
- 待补齐项：设备 A 仅缺 `Demo` 项目及 `demo-foreman`。设备 B 已克隆 AIWorkflow 唯一根仓，但尚未通过正式能力登记项目或补齐 13 个 AIWorkflow 任务；因此两端仍未完整应用项目清单版本 `9` 和任务清单版本 `5`。
- 来源状态：`Demo` 两个仓库尚无可用 HEAD；`funhub-space` 的 `english-talk-trainer` 与 `MountainFruitCottage` 远端返回 `Repository not found`；不创建空壳或假目录。
- 布局状态：设备 A 已发布并部分应用共享侧边栏布局版本 `2`；`vid-mat-lab` 已进入共享置顶区，39 任务目标中的 38 个 A 端现存实体已经正式接口确认为非置顶。`Demo` 保留逻辑末位，完整任务顺序仍未晋升为新的有效布局，所以不能记为完整应用。
- 待重试项：每次“开始同步”先读取最近未完成运行与完全对齐交接包；继续重试 Demo、两个 funhub 来源、B 端 AIWorkflow 登记与 13 个镜像、B 快照、唯一合并包、新布局和双端回执；不能把 A 端已完成的部分误报为双端完成。
