# 设备 A 已应用清单版本

- 项目清单版本：4
- 目标项目清单版本：9
- 任务清单版本：4
- 目标任务清单版本：5
- 完全对齐交接包版本：协议 `1`、目标修订 `1`；待设备 A 下次“开始同步”实际拉取、生成 A 快照并回执
- 设备状态确认来源与时间：设备 A 正式项目/任务列表与逐仓复核，2026-08-01 22:10（Asia/Shanghai）
- 已完整应用侧边栏布局版本：0
- 已部分应用侧边栏布局版本：2
- 本设备已确认待应用侧边栏布局版本：无
- 已部分应用未完成逻辑 ID：版本 `2` 的 `demo`
- 本机上次确认布局基线：版本 `2`；快照 `同步清单/侧边栏布局候选/1-A-20260731T180051Z-vid-pin.md`；规范化 SHA-256 `9572bd1210a264d270ddadee924bfb626efee445e663413fd9f008a7c077ad12`；比较时过滤 `demo`
- 项目清单未完成逻辑 ID：`demo`
- 应用日期：2026-08-01
- 状态：部分应用；任务清单版本 `4` 是旧长期任务基线，版本 `5` 的完整侧边栏任务并集尚未应用；Demo 与两个 funhub 子仓库仍受远端来源阻塞
- 策略：只增不减
- 已完成项：`CodexMissionContext`、`AI工作流（试点）`、`lottery`、`vid-mat-lab`、`Aidrama`、`canvas-garment`、`funhub-space` 和三个 liquidity 项目已出现在设备 A 左侧并回写真实 `projectId`；任务清单版本 `4` 曾完整映射。新目标版本 `5` 已登记 B 端新增任务和 AIWorkflow 全部现存任务，待 A 正式任务列表回读并补齐镜像。
- 待补齐项：设备 A 仍缺 `Demo` 项目；`AI工作流（试点）` 的唯一根仓来源已就绪，设备 B 尚未克隆和登记，因此两端仍未完整应用版本 `9`。
- 来源状态：`Demo` 两个仓库尚无可用 HEAD；`funhub-space` 的 `english-talk-trainer` 与 `MountainFruitCottage` 远端返回 `Repository not found`；不创建空壳或假目录。
- 布局状态：设备 A 已发布并部分应用共享侧边栏布局版本 `2`；`vid-mat-lab` 已进入共享置顶区，`Demo` 保留上一版本的逻辑末位，因此不能记为完整应用。
- 待重试项：每次“开始同步”先读取完全对齐交接包，继续重试 Demo、两个 funhub 来源、B 端 AIWorkflow 接入、A 端缺失任务镜像、逐任务同摘要交接和完整任务布局；生成 `同步运行/<运行ID>/A-任务快照.md` 与 `A-回执.md`，不能把目标版本误报为完整应用。
