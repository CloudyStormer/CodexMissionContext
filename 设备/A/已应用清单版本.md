# 设备 A 已应用清单版本

- 项目清单版本：4
- 目标项目清单版本：9
- 任务清单版本：4
- 目标任务清单版本：5
- 完全对齐交接包版本：协议 `1`、目标修订 `1`；运行 `A-20260802T200017+0800` 的 A/B 快照和唯一合并任务包已齐，39 项全部 `READY`，有效布局已晋升为版本 `3`，A 部分回执已更新，B 回执仍缺失
- 设备状态确认来源与时间：设备 A 正式项目/任务列表、双端快照、合并包与逐仓复核，2026-08-02（Asia/Shanghai）
- 已完整应用侧边栏布局版本：0
- 已部分应用侧边栏布局版本：3
- 本设备已确认待应用侧边栏布局版本：无
- 已部分应用未完成逻辑 ID：版本 `3` 的 `demo`、`demo-foreman`、项目排序/置顶、任务项目归属和完整 39 项共享子序列
- 本机上次确认布局基线：项目分量仍为版本 `2`；任务分量已部分应用版本 `3`，38 个现存任务正文、非置顶和共享子序列与目标第 1—38 位逐项一致；规范化目标 SHA-256 `5310e24b57cfc72e401f56e2e6d775351fa701fd8fb0d1f451b62ca92cb5d81e`
- 项目清单未完成逻辑 ID：`demo`
- 应用日期：2026-08-02
- 状态：部分应用；任务清单版本 `5` 的 39 项目标在 A 端已有 38 项真实实体，仅 `demo-foreman` 因 Demo 无 HEAD 且项目不存在而缺失；有效布局版本 `3` 的现存任务分量已应用和回读，但项目布局、任务项目归属及完整 39 项仍未通过，B 同摘要与 B 回执缺失
- 策略：只增不减
- 已完成项：`CodexMissionContext`、`AI工作流（试点）`、`lottery`、`vid-mat-lab`、`Aidrama`、`canvas-garment`、`funhub-space` 和三个 liquidity 项目已出现在设备 A 左侧并回写真实 `projectId`。A 端已把 21 个 B-only 任务补成镜像，回读任务清单版本 `5` 的 38 个现存实体全部可见、标题精确、非置顶，`unavailableHosts=[]`、`unavailableSources=[]`。AIWorkflow 已拉取到 `25d7ff5`。
- 待补齐项：设备 A 仅缺 `Demo` 项目及 `demo-foreman`。设备 B 已克隆 AIWorkflow 唯一根仓，但尚未通过正式能力登记项目或补齐 13 个 AIWorkflow 任务；因此两端仍未完整应用项目清单版本 `9` 和任务清单版本 `5`。
- 来源状态：`Demo` 两个仓库尚无可用 HEAD；`funhub-space` 的 `english-talk-trainer` 与 `MountainFruitCottage` 远端返回 `Repository not found`；不创建空壳或假目录。
- 布局状态：设备 A 已发布并部分应用共享侧边栏布局版本 `3`；39 个任务目标中的 38 个 A 端现存实体已确认为非置顶，共享子序列与目标第 1—38 位一致。项目排序/置顶、任务项目归属和缺失 Demo 尚未回读，因此不能记为完整应用。
- 待重试项：继续重试 Demo、两个 funhub 来源、B 端 AIWorkflow 登记与 13 个镜像；B 按版本 `3` 投递唯一合并正文并生成回执，两端再验收项目布局、任务项目归属和完整 39 项顺序；不能把 A 的部分应用误报为双端完成。
