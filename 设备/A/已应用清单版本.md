# 设备 A 已应用清单版本

- 项目清单版本：4
- 目标项目清单版本：9
- 任务清单版本：4
- 目标任务清单版本：5
- 完全对齐交接包版本：协议 `1`、目标修订 `1`；运行 `A-20260802T200017+0800` 的 A/B 快照和唯一合并任务包已齐，39 项全部 `READY`，有效布局已晋升为版本 `3`，A/B 回执仍未生成
- 设备状态确认来源与时间：设备 A 正式项目/任务列表、双端快照、合并包与逐仓复核，2026-08-02（Asia/Shanghai）
- 已完整应用侧边栏布局版本：0
- 已部分应用侧边栏布局版本：2
- 本设备已确认待应用侧边栏布局版本：3
- 已部分应用未完成逻辑 ID：版本 `3` 的 `demo`、`demo-foreman`、项目排序/置顶和完整任务共享子序列
- 本机上次确认布局基线：版本 `2`；快照 `同步清单/侧边栏布局候选/1-A-20260731T180051Z-vid-pin.md`；规范化 SHA-256 `9572bd1210a264d270ddadee924bfb626efee445e663413fd9f008a7c077ad12`；比较时过滤 `demo`
- 项目清单未完成逻辑 ID：`demo`
- 应用日期：2026-08-02
- 状态：部分应用；任务清单版本 `5` 的 39 项目标在 A 端已有 38 项真实实体，仅 `demo-foreman` 因 Demo 无 HEAD 且项目不存在而缺失；有效布局版本 `3` 已发布但尚未实际应用和回读，仍没有双端同摘要与双端回执
- 策略：只增不减
- 已完成项：`CodexMissionContext`、`AI工作流（试点）`、`lottery`、`vid-mat-lab`、`Aidrama`、`canvas-garment`、`funhub-space` 和三个 liquidity 项目已出现在设备 A 左侧并回写真实 `projectId`。A 端已把 21 个 B-only 任务补成镜像，回读任务清单版本 `5` 的 38 个现存实体全部可见、标题精确、非置顶，`unavailableHosts=[]`、`unavailableSources=[]`。AIWorkflow 已拉取到 `25d7ff5`。
- 待补齐项：设备 A 仅缺 `Demo` 项目及 `demo-foreman`。设备 B 已克隆 AIWorkflow 唯一根仓，但尚未通过正式能力登记项目或补齐 13 个 AIWorkflow 任务；因此两端仍未完整应用项目清单版本 `9` 和任务清单版本 `5`。
- 来源状态：`Demo` 两个仓库尚无可用 HEAD；`funhub-space` 的 `english-talk-trainer` 与 `MountainFruitCottage` 远端返回 `Repository not found`；不创建空壳或假目录。
- 布局状态：设备 A 已发布共享侧边栏布局版本 `3`，当前仅确认待应用；`vid-mat-lab` 的版本 `2` 置顶基线仍保留，39 个任务目标中的 38 个 A 端现存实体已确认为非置顶。项目排序/置顶、完整任务共享子序列与缺失 Demo 尚未回读，因此不能记为完整应用。
- 待重试项：继续重试 Demo、两个 funhub 来源、B 端 AIWorkflow 登记与 13 个镜像；按版本 `3` 向现存实体投递唯一合并正文并回读 SHA，应用共享布局，生成 A/B 回执；不能把已发布误报为已应用或双端完成。
