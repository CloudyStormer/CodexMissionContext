# 设备 A 已应用清单版本

- 项目清单版本：4
- 目标项目清单版本：9
- 任务清单版本：4
- 目标任务清单版本：5
- 完全对齐交接包版本：协议 `1`、目标修订 `1`；旧运行已晋升版本 `3`，B 后续回执为 `PARTIAL`。新运行 `A-20260804T180316+0800` 已发布 A 快照，B 本轮不可达，尚无新合并包或双端回执
- 设备状态确认来源与时间：设备 A 正式项目/任务列表、定向回读与逐仓复核，2026-08-04 18:12（Asia/Shanghai）
- 已完整应用侧边栏布局版本：0
- 已部分应用侧边栏布局版本：3
- 本设备已确认待应用侧边栏布局版本：无
- 已部分应用未完成逻辑 ID：版本 `3` 的 `demo`、`demo-foreman`、项目排序/置顶、任务项目归属和完整 39 项共享子序列
- 本机上次确认布局基线：项目分量仍为版本 `2`；任务实体与非置顶分量部分应用版本 `3`。38 个现存实体均非置顶，但近期活动使共享子序列偏离版本 `3`；规范化目标 SHA-256 仍为 `5310e24b57cfc72e401f56e2e6d775351fa701fd8fb0d1f451b62ca92cb5d81e`
- 项目清单未完成逻辑 ID：`demo`
- 应用日期：2026-08-04
- 状态：部分应用；任务清单版本 `5` 的 39 项目标在 A 端已有 38 项真实实体，仅 `demo-foreman` 因 Demo 无 HEAD 且项目不存在而缺失；有效布局版本 `3` 的实体与非置顶分量已部分应用，但项目布局、任务项目归属及完整 39 项仍未通过；本轮 B 快照、合并包与双端回执缺失
- 策略：只增不减
- 已完成项：A 的 10 个共同项目均有真实 `projectId`；38 个现存目标任务均可见、标题精确、非置顶，`unavailableHosts=[]`、`unavailableSources=[]`。AIWorkflow 已取得 `a35316d`，vid-mat-lab 已取得 `2b199d5`；18 个现存业务仓均 fetch，除 6 项明确冻结现场外与 upstream 为 `0/0`。
- 待补齐项：设备 A 仅缺 `Demo` 项目及 `demo-foreman`。设备 B 已正式登记 AIWorkflow 并补齐 13 个镜像；当前缺口改为本轮 B 快照、新合并正文、完整任务/项目布局回执和 A 的 Demo。
- 来源状态：`Demo` 两个仓库尚无可用 HEAD；`funhub-space` 的 `english-talk-trainer` 与 `MountainFruitCottage` 远端返回 `Repository not found`；不创建空壳或假目录。
- 布局状态：设备 A 已部分应用版本 `3`；38 个现存实体非置顶，但当前实际共享子序列已因近期业务活动偏离目标。项目排序/置顶、任务项目归属和缺失 Demo 尚未回读，因此不能记为完整应用。
- 待重试项：继续重试 Demo 与两个 funhub 来源；B 在同一运行 ID `A-20260804T180316+0800` 下生成本端快照，两端形成同版合并正文并回执，再验收项目布局、任务项目归属和完整 39 项顺序。不能把实体齐全或 A 的部分应用误报为双端完成。
