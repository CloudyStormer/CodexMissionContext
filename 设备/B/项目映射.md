# 设备 B 项目映射

记录日期：2026-07-28

| 逻辑项目 ID | 本地 projectId | 左侧名称 | 本地路径 |
|---|---|---|---|
| `mission-context` | `ef30d636-e54c-41df-8958-6e66834ca7d5` | CodexMissionContext | `D:\.aaProject-Bruce\CodexMissionContext` |

## 待补映射

- `lottery` 的前、后端仓库已完整克隆到 `D:\.aaProject-Bruce\lottery`。
- 当前 Codex 可调用接口不能新增左侧本地项目，且禁止用 Windows 界面自动化操作 Codex 桌面应用，因此尚无可记录的真实 `projectId`。
- 用户在左侧添加名称为 `lottery` 的本地项目后，必须读取真实 `projectId` 补入本表；不得复制设备 A 的 ID，也不得虚构空值为已完成。

