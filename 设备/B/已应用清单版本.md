# 设备 B 已应用清单版本

- 项目清单版本：5
- 目标项目清单版本：7
- 任务清单版本：4
- 目标任务清单版本：4
- 设备状态确认来源与时间：设备 B 正式项目/任务列表与逐仓复核，2026-08-01 17:50（Asia/Shanghai）
- 已完整应用侧边栏布局版本：0
- 已部分应用侧边栏布局版本：2
- 本设备已确认待应用侧边栏布局版本：无
- 已部分应用未完成逻辑 ID：版本 `2` 的 `ai-workflow`
- 本机上次确认任务置顶基线：版本 `2`；快照 `同步清单/侧边栏布局候选/1-A-20260731T180051Z-vid-pin.md`；规范化 SHA-256 `9572bd1210a264d270ddadee924bfb626efee445e663413fd9f008a7c077ad12`；两项共同长期任务均非置顶已验证
- 本机上次确认项目布局基线：版本 `0`；项目顺序与项目置顶尚未应用或验证，版本 `2` 候选摘要只作为目标规范，不能用于判断本机项目布局是否偏离
- 布局能力阻塞：当前会话没有正式项目排序/置顶 API，Windows Computer Use 禁止自动化 Codex 桌面应用
- 发布端待本设备获取侧边栏布局版本：无；设备 B 已于 2026-08-01 取得版本 `2`
- 发布端已知预期布局阻塞：无；以下均为设备 B 本次实际复核结果
- 项目清单未完成逻辑 ID：`ai-workflow`
- 应用日期：2026-08-01
- 状态：已取得项目清单版本 `7`、任务清单版本 `4` 和有效侧边栏布局版本 `2`；项目并集与布局均为部分应用
- 策略：只增不减
- 已完成项：当前项目列表中的 10 个 D 盘本地项目均已回读真实路径与 `projectId`；共同长期任务“双设备同步维护”和“差评死神”已完成本机映射；全局规则及 `aidrama`、`skillnotes`、`check-projects`、`daily-project-sync` 已进入本仓库。
- 设备 B 新发布项目：`AI 漫剧`、`画布成衣`、`花果山社区`、`演示 - 多垂直赛道（尚未收敛）`、`变现 - BMI`、`变现 - 证件照`、`变现 - 去水印`。
- Git 来源：除 `Demo` 两个尚无 HEAD/upstream 的仓库外，其余项目的 clone 地址已写入项目清单；`funhub-space` 当前有两个 fetch 失败和一个推送凭证阻塞。
- 版本 7 待补齐：`AI工作流（试点）` 根目录框架及两个子仓库没有普通 Git 来源；B 端不得以只有英语子仓库的目录冒充完整项目。
- 布局应用结果：正式任务列表确认当前没有置顶任务，两项共同长期任务均符合版本 `2` 的非置顶要求；本会话没有正式项目排序/置顶 API，Windows Computer Use 又明确禁止自动化 Codex 桌面应用，因此未执行项目整体重排，实际记为已部分应用版本 `2`。不得因本机部分视图回发残缺新版本。
- 2026-08-01 仓库复核：正式项目列表返回 10 个 D 盘本地共同项目和账号级云项目 `22`；共发现 22 个有效 Git 仓库，另有两个仅作为多仓项目容器的非 Git 根目录。SSH 远端可达的 Aidrama、canvas-garment、CodexMissionContext、两个 portrait 仓库、两个 watermark 仓库、lottery 前后端和 vid-mat-lab 成功 fetch；其中干净仓库均无可 fast-forward 新提交。8 个 funhub HTTPS 仓库及 BMI 前后端本轮连接 GitHub 443 超时；脏工作树全部保留，未 stash、暂存、提交或推送。
- 2026-08-01 16:20 项目提交复核：递归发现 24 个 `.git` 工作树，其中 `liquidity-portrait`、`liquidity-watermark-assistant` 根目录是无 HEAD、无 remote、无改动的空容器仓；实际有效范围仍为 21 个业务仓库加本上下文仓库。Aidrama、vid-mat-lab、canvas-garment 已成功 fetch，HEAD 与 upstream 均为 `0/0`；其余 18 个业务仓库因当前任务无 `.git` 写权限，且 SSH 主机校验或 HTTPS 凭证不可用，未能刷新远端。
- 本轮用户同时明确“开始提交、开始同步、开始上传”，已授权普通业务改动提交流程。主同步任务无业务仓库写权限；随后由三个项目自己的 D 盘任务创建 5 个本地提交：去水印后端 `5ad0017`、前端 `6a8f6ac`，证件照后端 `02560c1`，BMI 后端 `37ee234`、前端 `7a0caf6`。5 个提交均因 SSH 主机校验或 GitHub 443 阻塞尚未 push；其余改动保持原样。
- 共享布局复核：正式任务列表共 32 项、置顶 0、`unavailableHosts=[]`、`unavailableSources=[]`；两项共同长期任务继续非置顶。没有正式项目排序/置顶 API，且 `ai-workflow` 仍缺完整来源，因此保持部分应用版本 `2`，不从残缺视图发布新布局版本。
- 2026-08-01 17:50 完整同步复核：上下文从 `4b37f13` 快进到 `c02d036`；正式项目仍为 10 个 D 盘本地共同项目和云项目 `22`，无新增项目。21 个业务仓库中 19 个 fetch 成功，`english-talk-trainer` 与 `MountainFruitCottage` 明确返回 `Repository not found`；没有干净且落后的仓库可快进。`aime-bridge-backend` 唯一落后 1 个提交但有 19 项本地改动，按完整同步规则保留现场；5 个已验证本地提交及 `funhub` 的 2 个既有提交均未在“开始同步”模式上传。
- 差异记录：首次尝试曾观察到 `C:\Users\Administrator\Documents\lottery` 同名项目；最终项目列表未再返回该项，本同步流程未执行清理操作。
