# 项目：CodexMissionContext

最近更新：2026-07-31

## 一句话说明

为 A、B 两台设备提供平等的项目并集、可续接任务语义、完整同步口令和个人 Skill 持久化同步。

## 仓库

- 远程：`git@github.com:CloudyStormer/CodexMissionContext.git`
- 设备 A：`~/Desktop/DEV/Life/CodexSpace/CodexMissionContext`
- 设备 B：`D:\.aaProject-Bruce\CodexMissionContext`
- 分支：`main`

## 当前状态

- 设备 A 已取得设备 B 的上下文提交 `f4a6921`。本机旧 SSH 别名失效后，已把当前仓库和三个既有业务仓库的本地 remote 改用可验证为 `CloudyStormer` 的 `github.com-cloudystormer` 别名；标准公开克隆地址仍保留在清单。
- 设备 A 已把 `vid-mat-lab` 快进到 `c9f5919`；lottery 前后端、canvas-garment、六个可访问 funhub 仓库、三个 liquidity 项目的六个仓库均已取得远端且与 upstream 一致。
- 设备 B 发布的项目清单版本 `5` 中，设备 A 已正式登记 canvas-garment、funhub-space 和三个 liquidity 项目；Aidrama 约 1.5 GB 的完整 clone 正在进行，Demo 仍因远端没有 HEAD 而阻塞。
- 设备 A 新发现 `AI工作流（试点）`，项目并集升至版本 `6`。其根目录不是 Git 仓库，只有 `projects/ai-english-learning` 有远端来源；设备 B 尚不能完整重建根目录框架。
- 四字口令“开始同步”新增共享侧边栏布局同步：发布逻辑项目顺序、项目置顶和共同长期任务置顶；另一端落后时先应用远端版本，且必须稳定保留个人/临时对象的置顶状态和相对顺序。
- 共享侧边栏布局版本 `1` 由设备 A 发布：`mission-context`、`ai-workflow` 依次置顶，其余共同项目按清单顺序排列；共同长期任务当前均非置顶。A、B 都因缺失并集项目而只能部分应用。
- 设备 A 的正式任务列表最新返回 22 个可见任务，`unavailableHosts` 和 `unavailableSources` 均为空；当天 `AI工作流（试点）` 下新增“包工头”、项目经理及九个专业角色任务，均未被用户指定为双设备共同长期任务。另一台离线设备未推送的本地状态仍不可见。
- 另一任务并行执行结束同步时，把本任务的部分未提交规则误纳入 `cb51910`、`50c9adc` 并推送；历史保留不改写，已通知该任务暂停提交本仓库的未提交变化。

## 已确认决定

- A、B 没有主次关系；哪台设备被使用，哪台就先拉取、继续并及时推送。
- Windows 共同项目只放在 `D:\.aaProject-Bruce\`，不在 C 盘创建共同项目或 worktree。
- 项目代码由各自 Git 仓库管理；本仓库只保存精准压缩上下文、清单、映射和 Skill。
- 日常同步不逐文件研究业务差异，也不提交原任务尚未确认的改动。
- “开始同步”要求尽可能完整拉取，但不能突破真实权限、凭证、冲突和离线边界；不可达部分必须登记并重试。
- “开始同步”不授权 stash、暂存、提交或推送脏业务仓库；只对干净且可 fast-forward 的工作树拉取。
- 共享布局只保存逻辑 ID 和版本，通过正式 API 或用户可见界面稳定合并；不复制或写入 Codex 原始状态、数据库或 session。
- `AI学习` 永久排除，除非用户以后明确改变范围。

## 当前阻塞

- `Aidrama` 完整 clone 尚未结束；完成前不能正式登记项目或创建 A 端“差评死神”长期任务。
- `Demo` 两个远端存在但没有任何 HEAD 引用，不能创建空壳项目。
- `funhub-space/english-talk-trainer` 和 `MountainFruitCottage` 在设备 A 以 `CloudyStormer` 身份读取时返回 `Repository not found`；设备 B 已有的本地目录不得删除。
- 设备 B 的 `funhub` 仍有 2 个未推送本地提交；设备 A 只能看见远端基线，不能替代 B 推送。
- `AI工作流（试点）` 根目录框架缺少 Git 来源；其子仓库由其他任务新增本地提交 `8dd9822`（领先远端 1 个）并保留 11 个未提交 Skill 修改，本任务未覆盖、未暂存、未提交、未推送。
- 设备 A 缺少 `Demo`，设备 B 缺少 `ai-workflow`，因此侧边栏布局版本 `1` 在两端都只能部分应用。

## 精确下一步

1. 等待 Aidrama 完整 clone，验证 HEAD/upstream/工作树，通过正式项目入口登记并创建 A 端“差评死神”共同任务。
2. 设备 B 下次“开始同步”先拉取版本 `6` 和侧边栏布局版本 `1`；在 `ai-workflow` 来源未完整前只稳定应用可执行部分，不发布残缺布局。
3. 为 `AI工作流（试点）` 根目录提供真实 Git 来源后，设备 B 才在 `D:\.aaProject-Bruce\AIWorkFlow` 建立完整项目。
4. 修复或更正两个不可访问的 funhub 仓库来源；设备 B 由原任务推送 `funhub` 的 2 个本地提交。
5. `Demo` 的前后端由原业务任务确认初始内容、验证并首次提交后，再由设备 A 克隆并放到布局末尾。
