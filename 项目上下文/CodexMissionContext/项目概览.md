# 项目：CodexMissionContext

最近更新：2026-08-04

## 一句话说明

为 A、B 两台设备提供平等的项目并集、可续接任务语义、完整同步口令和个人 Skill 持久化同步。

## 仓库

- 远程：`git@github.com:CloudyStormer/CodexMissionContext.git`
- 设备 A：`~/Desktop/DEV/Life/CodexSpace/CodexMissionContext`
- 设备 B：`D:\.aaProject-Bruce\CodexMissionContext`
- 分支：`main`

## 当前状态

- 设备 B 已取得设备 A 发布的 AIWorkflow 唯一根仓来源并克隆到规定 D 盘路径；设备 A 使用可验证为 `CloudyStormer` 的 `github.com-cloudystormer` SSH 别名，清单仍保存标准公开地址。
- 项目并集清单为版本 `9`。Aidrama、canvas-garment、funhub-space、三个 liquidity 项目和 AIWorkflow 唯一根仓均已登记；设备 A 只因 Demo 无 HEAD 不能越过完整应用版本 `4`。
- Aidrama 已完整取得到正式路径：Git 929 个对象 `missing=0`、严格 fsck 通过；全历史 LFS 60 个对象、`967,655,248` 字节已全部校验，`main` / `3baedae5d3bb` 与 upstream 一致且工作树干净。
- 设备 A 在运行 `A-20260804T180316+0800` fetch 18 个现存业务仓库，并快进取得证件照前端 `4f1290d` 与 funhub-taro `ba8fb21`。最终 17 仓 clean、ahead/behind `0/0`；AIWorkflow 为 `a35316d`、upstream `0/0`，但保留 6 项被项目工作流冻结的 English 前端现场。vid-mat-lab 为 `2b199d5`。
- `AI工作流（试点）` 是唯一根仓 `AI-Workflow-Dev`，只有根部一个 `.git` 和一个 `origin`。A 为 `a35316d`；B 已在 `D:\.aaProject-Bruce\AIWorkFlow` 正式登记 `projectId=e7d65417-ea9a-47b3-8e2f-87da309b8406` 并补齐 13 个任务，最近代码回执为 `c8ffa9e`，待下次取得 A 的新提交。
- 设备 A 有任务清单版本 `5` 的 38/39 个实体，仅缺 `demo-foreman`；两个曾不可见目标任务已恢复可见，38 项均非置顶。设备 B 已有 39/39 个实体。旧运行 B 回执仍为 `PARTIAL`；新运行只取得 A 快照，没有 B 快照、合并包或双端回执。
- 共享侧边栏布局有效版本仍为 `3`。旧运行曾验证 A 的 38 项子序列；2026-08-04 正常业务活动已改变 A 当前实际顺序。B 的 39 个实体和非置顶已确认，但两端完整任务顺序、项目顺序/置顶和新正文仍未完成机器验收。
- 全局 `xiaoneihao-video-creator` Skill 已进入本仓库，设备 A 的全局软链接已回读正确；设备 B 需在下次拉取后安装并回读 Junction，未验证前不声称 B 已启用。
- 完全对齐交接包协议 `1`、目标修订 `1` 已把当前 11 个共同项目和 39 个逻辑任务的唯一目标子序列、任务语义正文结构、SHA-256 校验、运行快照与双端回执固化。完整任务顺序已升级为有效布局版本 `3`；旧运行中曾验证 A 端 38 个现存任务子序列，但 2026-08-04 当前顺序已因正常业务活动偏离，不冒充现行完整双端应用事实。
- 布局协议允许发布端在缺失对象沿用上一版本逻辑位置、当前可见变化明确且不存在同基线冲突时发布下一版本；明确排除对象的置顶状态和相对顺序必须稳定保留。共同项目内的临时任务从版本 `5` 起也属于共享对象。

## 已确认决定

- A、B 没有主次关系；哪台设备被使用，哪台就先拉取、继续并及时推送。
- Windows 共同项目只放在 `D:\.aaProject-Bruce\`，不在 C 盘创建共同项目或 worktree。
- 项目代码由各自 Git 仓库管理；本仓库只保存精准压缩上下文、清单、映射和 Skill。
- 日常同步不逐文件研究业务差异，也不提交原任务尚未确认的改动。
- 每台设备的同步与提交统一由一个维护任务直接操作本机各仓库，不再把 Git 动作分派到独立项目任务并逐项要求用户确认；A、B 在同一运行 ID 下各执行一个维护任务不属于逐项目分派。
- “开始同步”是两端同轮、全部共同项目与任务的完整双向同步：若对端维护任务可达则只唤醒一次；两端各自先完成远端取得与安全对齐，再审查、验证、提交和推送安全的本地普通改动，并验证本地与 upstream 一致。
- “开始同步”还必须把两端同步范围任务取并集，在对应已保存项目补齐全部可见镜像，逐任务发送可直接续接的精简语义交接，并验证标题、项目归属、共享顺序和置顶。只有取得同一运行 ID 的两端成功报告，且代码、任务实体、语义和布局全部验证成功，才可声称两端全部同步。真实权限、凭证、冲突、验证失败、敏感内容、业务歧义和离线边界仍须如实登记，禁止强行上传或伪造任务完成。
- 共享布局只保存逻辑 ID 和版本，通过正式 API 或用户可见界面稳定合并；不复制或写入 Codex 原始状态、数据库或 session。
- 每次“开始同步”都在 `同步运行/<运行ID>/` 由 A、B 分别发布本机任务快照，发起端生成唯一合并任务包，两端发送相同正文并用 SHA-256 回执验收；日报不能代替这一运行级交接。
- 完整任务布局先取得 A/B 快照和合并包；目标未变时直接应用并验收现有有效版本 `3`，只在任务并集或用户明确布局变更时才走“完整任务并集迁移”候选与两阶段晋升。最后应用并回读任务正文重算 SHA；发送成功本身不构成内容回执。
- `AI学习`、`AI 教学助手`、`AI 笔记助手` 永久排除，除非用户以后明确改变范围。

## 当前阻塞

- `Demo` 两个远端存在但没有任何 HEAD 引用，不能创建空壳项目。
- `funhub-space/english-talk-trainer` 和 `MountainFruitCottage` 在设备 A 以 `CloudyStormer` 身份读取时返回 `Repository not found`；设备 B 已有的本地目录不得删除。
- 设备 B 已验证并普通推送去水印后端 `5ad0017`、前端 `6a8f6ac`、证件照后端 `02560c1`、BMI 后端 `37ee234`、前端 `7a0caf6` 及 funhub `5111563`；设备 A 已快进取得，六仓均与 upstream 为 `0/0`。
- `funhub-WordSmiths-backend/.env` 当前含真实秘密，`aime-bridge-backend/.env` 当前为空但历史曾含非空敏感值；二者仍被远端跟踪且无 ignore 覆盖，具体值未进入上下文。`funhub-WordSmiths-backend/app/data/topic_chat_sessions.json` 还是已跟踪的运行时聊天/会话数据。必须先轮换/作废历史凭据，再由业务任务停止跟踪环境文件和会话数据、保留脱敏样例；普通同步不强推改写历史。
- `AI工作流（试点）` 已在 B 正式登记并补齐 13 个任务；当前差异是 B 最近代码基线 `c8ffa9e` 落后于远端 `a35316d`，且 2026-08-04 新语义尚未生成 B 快照和合并正文。
- 有效布局版本 `3` 已发布；A 当前 38 个现存任务均非置顶但实际顺序因业务活动偏离，B 的 39 个实体非置顶但完整共享索引未知。Demo 缺失、项目布局与任务正文回执仍阻塞完成。
- 项目接口没有排序/置顶或任务项目 ID 回读，Computer Use 安全策略禁止控制 Codex App；因此项目布局和任务项目归属仍为 `unknown`，不能写数据库绕过。
- 设备 B 最新一轮已成功 fetch 19 个业务仓库；此前 GitHub 443 批量连接阻塞已解除。仍不可达的只有 `english-talk-trainer` 与 `MountainFruitCottage`，两者均明确返回 `Repository not found`。
- 旧运行 `A-20260802T200017+0800` 的单次唤醒与检查点 `ab05b97` 推送后的单次结果通知均返回 `No Codex thread found`。新运行 `A-20260804T180316+0800` 的唯一一次 B 唤醒也返回同样结果；按协议不重试。因此本轮只能确认设备 A 的现场，不能声称双端同轮完成。

## 精确下一步

1. 设备 B 下次收到“开始同步”先拉取 `同步运行/A-20260804T180316+0800/` 和最新 AIWorkflow；直接使用现有 D 盘项目及 13 个任务，不重复克隆或创建。
2. B 按当前 39 项目标生成新的完整快照；两端快照齐后由发起端生成唯一合并包，向 39 个既有 B 实体与 38 个 A 实体投递同版正文并回读 SHA、置顶和顺序。
3. 设备 A 继续重试 Demo；项目排序/置顶能力恢复前不得用原始状态文件或数据库绕过。A 的 English 冻结工作树必须等待项目审批链恢复，不能由同步任务提交。
4. 业务任务修复 `funhub` 两处行尾空白；立即轮换/作废已进入 Git 的凭据，再停止跟踪两个后端 `.env` 和运行时会话 JSON，补充 ignore 与脱敏模板。如需改写远端历史，必须另行制定方案并明确授权，不能在普通同步中强推。
5. 修复或更正两个不可访问的 funhub 仓库来源。
6. `Demo` 的前后端由原业务任务确认初始内容、验证并首次提交后，再由设备 A 克隆并放到布局末尾。

## 2026-08-02 设备 B 继续运行 `A-20260802T200017+0800`

- B 已拉取 A 的最新上下文并正式回读本机 10 个 D 盘共同项目、无置顶的任务列表及 26 个目标范围 D 盘实体；没有发现第 40 个共同任务或目标版本变化。
- B 在正式 D 盘 `vid-mat-lab` 项目补建“包工头 (2)”镜像 `019fc2a5-a242-7401-a3e5-dcacecc55016`，替换共同映射中的旧 C 盘 worktree 线程；旧线程保留原样。
- 22 个业务仓库中 20 个完成远端取得；两个不可达来源仍为 `english-talk-trainer` 与 `MountainFruitCottage`。去水印前后端、证件照后端、BMI 前后端和 funhub 共 6 仓的既有提交已验证并普通推送，最终均为 upstream `0/0`。
- 其他脏仓因冲突、未完成内容、异常大文件、生成垃圾、环境/凭据风险或首次提交歧义而保留。`aime-bridge-backend` 在保护性 stash、rebase 后恢复时冲突于 `app/services/chat_store.py`，stash 未丢弃，未自动合并或上传。
- 该历史阶段 B 仍无法把现有 `D:\.aaProject-Bruce\AIWorkFlow` 登记进正式项目列表，因此当时 13 个任务镜像缺失；也没有项目任意排序/置顶能力。B 已发布并校验当时的 39 项快照（26 `PRESENT`、13 `MISSING`，整文件 SHA-256 `f98ddf1597489cb4f27783b6604480644e990e6e9fc8a8a8027533a6535dc4cb`）。后续 B 正式登记和 13 个镜像补齐已完成，当前状态以本文件顶部与 2026-08-04 新运行为准。
