# 项目：CodexMissionContext

最近更新：2026-08-01

## 一句话说明

为 A、B 两台设备提供平等的项目并集、可续接任务语义、完整同步口令和个人 Skill 持久化同步。

## 仓库

- 远程：`git@github.com:CloudyStormer/CodexMissionContext.git`
- 设备 A：`~/Desktop/DEV/Life/CodexSpace/CodexMissionContext`
- 设备 B：`D:\.aaProject-Bruce\CodexMissionContext`
- 分支：`main`

## 当前状态

- 设备 B 已从 `4b37f13` 快进取得设备 A 的最新上下文提交 `c02d036`，并再次确认项目清单版本 `7`、任务清单版本 `4` 和有效布局版本 `2`。设备 A 使用可验证为 `CloudyStormer` 的 `github.com-cloudystormer` SSH 别名，清单仍保存标准公开地址。
- 项目并集清单为版本 `7`。Aidrama、canvas-garment、funhub-space、三个 liquidity 项目均已正式登记；设备 A 只因 Demo 无 HEAD 不能越过完整应用版本 `4`。
- Aidrama 已完整取得到正式路径：Git 929 个对象 `missing=0`、严格 fsck 通过；全历史 LFS 60 个对象、`967,655,248` 字节已全部校验，`main` / `3baedae5d3bb` 与 upstream 一致且工作树干净。
- 设备 A 本轮再次逐仓复核 20 个可访问业务仓库：全部干净；18 个有 upstream 的仓库 fetch 成功且 ahead/behind 均为 `0/0`，AI Model Radar 与 Control Center 只有本地干净提交但没有普通 Git remote。
- `AI工作流（试点）` 根目录不是 Git 仓库，内含英语、AI Model Radar、Control Center 三个独立子仓；只有英语项目有普通 Git 远端。设备 A 有 12 个固定编号角色任务，均非置顶、跨项目复用且未被指定为共同长期任务。
- 设备 A 正式任务列表最新返回 24 个可见任务，`unavailableHosts=[]`、`unavailableSources=[]`；共同长期任务“双设备同步维护”和“差评死神”已在 A、B 两端完成映射。
- 共享侧边栏布局版本 `2` 由设备 A 发布：`mission-context`、`ai-workflow`、`vid-mat-lab` 依次置顶；其余共享项目按布局文件的明确顺序排列，两项共同长期任务均非置顶。A 因缺少 Demo 为部分应用；B 已取得并实际尝试版本 `2`，任务置顶分量已验证，但因缺少 `ai-workflow` 且没有正式项目排序/置顶能力，项目布局仍未应用，整体同样记为部分应用。
- 布局协议允许发布端在缺失对象沿用上一版本逻辑位置、当前可见变化明确且不存在同基线冲突时发布下一版本；个人/临时对象的置顶状态和相对顺序仍必须稳定保留。

## 已确认决定

- A、B 没有主次关系；哪台设备被使用，哪台就先拉取、继续并及时推送。
- Windows 共同项目只放在 `D:\.aaProject-Bruce\`，不在 C 盘创建共同项目或 worktree。
- 项目代码由各自 Git 仓库管理；本仓库只保存精准压缩上下文、清单、映射和 Skill。
- 日常同步不逐文件研究业务差异，也不提交原任务尚未确认的改动。
- 每次同步与提交统一由当前同步任务直接操作各仓库，不再把 Git 动作分派到独立项目任务并逐项要求用户确认；不可避免的权限阻塞只合并报告一次。
- “开始同步”要求尽可能完整拉取，但不能突破真实权限、凭证、冲突和离线边界；不可达部分必须登记并重试。
- “开始同步”不授权 stash、暂存、提交或推送脏业务仓库；只对干净且可 fast-forward 的工作树拉取。
- 共享布局只保存逻辑 ID 和版本，通过正式 API 或用户可见界面稳定合并；不复制或写入 Codex 原始状态、数据库或 session。
- `AI学习` 永久排除，除非用户以后明确改变范围。

## 当前阻塞

- `Demo` 两个远端存在但没有任何 HEAD 引用，不能创建空壳项目。
- `funhub-space/english-talk-trainer` 和 `MountainFruitCottage` 在设备 A 以 `CloudyStormer` 身份读取时返回 `Repository not found`；设备 B 已有的本地目录不得删除。
- 设备 B 有 5 个已验证但尚未推送的本地提交：去水印后端 `5ad0017`、前端 `6a8f6ac`，证件照后端 `02560c1`，BMI 后端 `37ee234`、前端 `7a0caf6`；设备 A 尚不能从远端取得这些提交。
- 设备 B 的 `funhub` 仍有 2 个未推送本地提交；设备 A 只能看见远端基线，不能替代 B 推送。
- funhub 两个后端的语音服务凭据已经进入既有 Git 历史；删除被跟踪的 `.env` 不能撤销泄露，必须在服务端立即轮换凭据。
- `AI工作流（试点）` 根目录框架、AI Model Radar 和 Control Center 缺少普通 Git 来源；设备 B 只能取得英语子仓，不能冒充完整项目。
- 设备 A 缺少 `Demo`，所以布局版本 `2` 只能部分应用；设备 B 缺少完整 `ai-workflow`，且当前没有正式项目排序/置顶能力，因此项目布局分量仍未应用。
- 设备 B 最新一轮已成功 fetch 19 个业务仓库；此前 GitHub 443 批量连接阻塞已解除。仍不可达的只有 `english-talk-trainer` 与 `MountainFruitCottage`，两者均明确返回 `Repository not found`。

## 精确下一步

1. 为 `AI工作流（试点）` 根目录、AI Model Radar 和 Control Center 提供真实 Git 来源后，设备 B 才在 `D:\.aaProject-Bruce\AIWorkFlow` 建立完整项目。
2. 设备 B 在正式项目排序/置顶能力可用时，按版本 `2` 应用项目布局；能力恢复前不得用原始状态文件或数据库绕过限制。
3. 设备 B 下次进入“项目提交”或明确上传模式时，优先推送 5 个已验证本地提交和 `funhub` 的 2 个既有提交；当前“开始同步”只刷新远端，不上传业务提交。脏工作树继续保留现场。
4. 立即在服务端轮换已进入 funhub Git 历史的语音服务凭据，再由业务任务安全提交 `.env` 停止跟踪改动；不得在上下文中保存凭据值。
5. 修复或更正两个不可访问的 funhub 仓库来源；设备 B 由原任务推送 `funhub` 的 2 个本地提交。
6. `Demo` 的前后端由原业务任务确认初始内容、验证并首次提交后，再由设备 A 克隆并放到布局末尾。
