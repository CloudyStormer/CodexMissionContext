# 项目：CodexMissionContext

最近更新：2026-08-01

## 一句话说明

为 A、B 两台设备提供平等的项目并集、可续接任务语义、完整同步口令和个人 Skill 持久化同步。

## 仓库

- 远程：`git@github.com:CloudyStormer/CodexMissionContext.git`
- 设备 A：`~/Desktop/DEV/Life/CodexSpace/CodexMissionContext`
- 设备 B：`D:\.aaProject-Bruce\CodexMissionContext`
- 分支：`main`

## 当前状态

- 设备 A 本轮从远端最新上下文 `4f57b4a` 开始；设备 B 最近已发布“开始同步”双向升级规则，但尚未确认取得设备 A 后续发布的 AIWorkflow 唯一根仓来源。设备 A 使用可验证为 `CloudyStormer` 的 `github.com-cloudystormer` SSH 别名，清单仍保存标准公开地址。
- 项目并集清单为版本 `9`。Aidrama、canvas-garment、funhub-space、三个 liquidity 项目和 AIWorkflow 唯一根仓均已登记；设备 A 只因 Demo 无 HEAD 不能越过完整应用版本 `4`。
- Aidrama 已完整取得到正式路径：Git 929 个对象 `missing=0`、严格 fsck 通过；全历史 LFS 60 个对象、`967,655,248` 字节已全部校验，`main` / `3baedae5d3bb` 与 upstream 一致且工作树干净。
- 设备 A 本轮逐仓复核 18 个可访问业务仓库：全部 fetch 成功、工作树干净且 ahead/behind 均为 `0/0`，没有可拉取或可推送的本机普通改动。
- `AI工作流（试点）` 已恢复为唯一根仓 `AI-Workflow-Dev`：全目录只有根部一个 `.git`、无 gitlink，英语、AI Model Radar 和 Control Center 均为根仓普通目录；设备 A 与远端均为 `main` / `fa2294c`。设备 A 有 12 个固定编号角色任务，均非置顶、跨项目复用且未被指定为共同长期任务。
- 设备 A 正式任务列表最新返回 24 个可见任务，`unavailableHosts=[]`、`unavailableSources=[]`；共同长期任务“双设备同步维护”和“差评死神”已在 A、B 两端完成映射。
- 共享侧边栏布局版本 `2` 由设备 A 发布：`mission-context`、`ai-workflow`、`vid-mat-lab` 依次置顶；其余共享项目按布局文件的明确顺序排列，两项共同长期任务均非置顶。A 因缺少 Demo 为部分应用；B 已取得并实际尝试版本 `2`，任务置顶分量已验证，但因缺少 `ai-workflow` 且没有正式项目排序/置顶能力，项目布局仍未应用，整体同样记为部分应用。
- 布局协议允许发布端在缺失对象沿用上一版本逻辑位置、当前可见变化明确且不存在同基线冲突时发布下一版本；个人/临时对象的置顶状态和相对顺序仍必须稳定保留。

## 已确认决定

- A、B 没有主次关系；哪台设备被使用，哪台就先拉取、继续并及时推送。
- Windows 共同项目只放在 `D:\.aaProject-Bruce\`，不在 C 盘创建共同项目或 worktree。
- 项目代码由各自 Git 仓库管理；本仓库只保存精准压缩上下文、清单、映射和 Skill。
- 日常同步不逐文件研究业务差异，也不提交原任务尚未确认的改动。
- 每台设备的同步与提交统一由一个维护任务直接操作本机各仓库，不再把 Git 动作分派到独立项目任务并逐项要求用户确认；A、B 在同一运行 ID 下各执行一个维护任务不属于逐项目分派。
- “开始同步”是两端同轮、全部共同项目的完整双向同步：若对端维护任务可达则只唤醒一次；两端各自先完成远端取得与安全对齐，再审查、验证、提交和推送安全的本地普通改动，并验证本地与 upstream 一致。只有取得同一运行 ID 的两端成功报告，才可声称两端全部同步。
- “开始同步”还必须同步所有可见共同项目对话的完整语义；临时对话写入同步报告而不复制成另一端任务。真实权限、凭证、冲突、验证失败、敏感内容、业务歧义和离线边界仍须如实登记，禁止强行上传。
- 共享布局只保存逻辑 ID 和版本，通过正式 API 或用户可见界面稳定合并；不复制或写入 Codex 原始状态、数据库或 session。
- `AI学习`、`AI 教学助手`、`AI 笔记助手` 永久排除，除非用户以后明确改变范围。

## 当前阻塞

- `Demo` 两个远端存在但没有任何 HEAD 引用，不能创建空壳项目。
- `funhub-space/english-talk-trainer` 和 `MountainFruitCottage` 在设备 A 以 `CloudyStormer` 身份读取时返回 `Repository not found`；设备 B 已有的本地目录不得删除。
- 设备 B 最近一次已推送的交接记录称，当时有 5 个已验证但尚未推送的本地提交：去水印后端 `5ad0017`、前端 `6a8f6ac`，证件照后端 `02560c1`，BMI 后端 `37ee234`、前端 `7a0caf6`；设备 A 尚不能从远端确认这些离线现场当前是否仍存在。
- 同一历史交接还记录了 `funhub` 的 2 个未推送本地提交；设备 A 只能看见远端基线，不能替代 B 验证或推送。
- `funhub-WordSmiths-backend` 当前远端基线仍跟踪 `.env`，其中检测到 3 个非空非占位的敏感值；值未进入上下文。凭据已经进入既有 Git 历史，必须立即在服务端轮换并由业务任务停止跟踪 `.env`。
- `AI工作流（试点）` 唯一根仓来源已经就绪，但设备 B 尚未克隆到 `D:\.aaProject-Bruce\AIWorkFlow` 并登记真实项目。
- 设备 A 缺少 `Demo`，所以布局版本 `2` 只能部分应用；设备 B 缺少完整 `ai-workflow`，且当前没有正式项目排序/置顶能力，因此项目布局分量仍未应用。
- 设备 B 最新一轮已成功 fetch 19 个业务仓库；此前 GitHub 443 批量连接阻塞已解除。仍不可达的只有 `english-talk-trainer` 与 `MountainFruitCottage`，两者均明确返回 `Repository not found`。
- 本轮同步运行 `A-20260801T220415+0800` 尝试一次性唤醒设备 B 维护任务时，当前 Codex 返回 `No Codex thread found`；因此只能确认设备 A 的现场，不能声称双端同轮完成。

## 精确下一步

1. 设备 B 下次同步从唯一根仓 `git@github.com:CloudyStormer/AI-Workflow-Dev.git` 克隆 `main` / `fa2294c` 到 `D:\.aaProject-Bruce\AIWorkFlow`，验证后登记真实 `projectId`。
2. 设备 B 在正式项目排序/置顶能力可用时，按版本 `2` 应用项目布局；能力恢复前不得用原始状态文件或数据库绕过限制。
3. 设备 B 下次收到“开始同步”时，按新定义先取远端，再核验历史记录中的 5 个本地提交和 `funhub` 的 2 个既有提交；若仍存在且安全验证通过再推送，其余脏工作树只提交验证安全且语义明确的内容。
4. 立即在服务端轮换已进入 funhub Git 历史的语音服务凭据，再由业务任务安全提交 `.env` 停止跟踪改动；历史清理如需改写远端必须另行制定并明确授权，不能在普通同步中强推。
5. 修复或更正两个不可访问的 funhub 仓库来源；设备 B 由原任务推送 `funhub` 的 2 个本地提交。
6. `Demo` 的前后端由原业务任务确认初始内容、验证并首次提交后，再由设备 A 克隆并放到布局末尾。
