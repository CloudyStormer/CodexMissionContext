# 项目：CodexMissionContext

最近更新：2026-07-30

## 一句话说明

为 A、B 两台设备提供平等的项目并集、可续接任务语义、完整同步口令和个人 Skill 持久化同步。

## 仓库

- 远程：`git@github.com:CloudyStormer/CodexMissionContext.git`
- 设备 A：`~/Desktop/DEV/Life/CodexSpace/CodexMissionContext`
- 设备 B：`D:\.aaProject-Bruce\CodexMissionContext`
- 分支：`main`

## 当前状态

- 设备 B 已拉取设备 A 当天新增的 9 个上下文提交：先取得 `fa11664` 交班批次，再取得 `45d62cb`、`0f6588b`、`f4b4826` 项目协作批次；双方并行修改通过 rebase 合并。完整同步记录已推送，`HEAD` 与 `origin/main` 一致。
- `vid-mat-lab` 已从 `cb82d20` 快进到设备 A 交班提交 `697db9f`，与 `origin/main` 一致；本机 9 项未确认工作树内容保持原样。
- 项目公共范围为两端可同步项目并集。设备 B 正式项目列表已回读 10 个 D 盘本地项目，项目清单升至版本 `5`；B 已完整映射，A 当前版本 `3`，待补齐 B 发布的 7 个新增项目。
- `check-projects` 与 `daily-project-sync` 两个 Skill 共同存在：前者处理主动完整同步、项目提交和交接；后者只做普通每日任务摘要和轻量 Git 同步。
- 四字口令“开始同步”已定义为完整同步：拉取上下文、刷新当前可访问的本地/远程/账号级线上任务状态、核对项目并集，并取得当前设备全部项目仓库的远端状态和可安全拉取的提交。
- 当前任务列表能力已返回 31 个可见任务（含 1 个置顶任务），`unavailableHosts` 和 `unavailableSources` 均为空；未连接的另一台离线设备尚未提交到 Git 的本地任务和文件仍不可见，不能误报为已拉取。

## 已确认决定

- A、B 没有主次关系；哪台设备被使用，哪台就先拉取、继续并及时推送。
- Windows 共同项目只放在 `D:\.aaProject-Bruce\`，不在 C 盘创建共同项目或 worktree。
- 项目代码由各自 Git 仓库管理；本仓库只保存精准压缩上下文、清单、映射和 Skill。
- 日常同步不逐文件研究业务差异，也不提交原任务尚未确认的改动。
- “开始同步”要求尽可能完整拉取，但不能突破真实权限、凭证、冲突和离线边界；不可达部分必须登记并重试。
- `AI学习` 永久排除，除非用户以后明确改变范围。

## 当前阻塞

- 设备 A 尚未拉取项目清单版本 `5`，需补齐 7 个由 B 发布的项目；`Demo` 两个仓库尚无 HEAD/upstream，不能以空壳补齐。
- `funhub-space/aime-bridge-backend` 落后远端 1 个提交且有未确认本地改动；本任务当前没有该目录写权限，无法创建保护 stash 后拉取。
- `funhub-space/english-talk-trainer` 与 `MountainFruitCottage` fetch 失败。
- `funhub-space/funhub` 有 2 个本地提交待推送，但 HTTPS push 返回 `SEC_E_NO_CREDENTIALS`；SSH 重试又被当前沙箱拒绝读取 `known_hosts`。

## 精确下一步

1. 本次上下文记录推送后，设备 A 执行“开始同步”，按项目清单版本 `5` 补齐来源可用的 7 个项目并回写映射。
2. 在具备项目目录写权限的任务中，为 `aime-bridge-backend` 的未确认改动创建包含未跟踪文件的命名 stash，快进拉取后恢复；冲突时保留 stash。
3. 修复 Windows GitHub HTTPS 凭证后推送 `funhub` 的 2 个既有提交，并重试两个 fetch 失败仓库。
4. `Demo` 的前后端由原业务任务确认初始内容、验证并首次提交后，再由另一端克隆。
5. 以后用户发送四字口令“开始同步”时，严格执行 `check-projects` 完整同步模式。
