# 任务：双设备同步维护

最近更新：2026-07-30
逻辑任务 ID：`mission-context-maintenance`

## 任务目的

让 A、B 两台设备通过私有 Git 仓库共享项目并集、长期任务清单、压缩后的任务语义、全局规则和个人 Skill；项目通过只增不减补齐到两端，额外任务继续保留但不自动同步。

## 用户确认的边界

- 双方左侧必须包含 A、B 可同步项目并集和任务清单规定的共同长期任务。
- 最近临时对话不需要统一，也不复制原始聊天全文。
- 对齐只能增加缺失项；不得为了统一而删除、归档、重命名或隐藏任何项目与任务。
- A、B 的 `projectId`、`threadId` 可以不同；名称、归属和上下文一致即可。
- A、B 只是设备标识，完全平等，不存在主设备、次设备或固定接力顺序；用户会交替使用，任一设备都必须随时拉取、接续、更新并推送。
- Windows 设备上的所有共同项目固定放在 `D:\.aaProject-Bruce\`，禁止放到 C 盘；共同任务直接使用 D 盘已保存项目，不在 C 盘创建 Codex worktree 或项目副本。
- 项目代码由各自 Git 仓库同步，本仓库不复制项目代码。
- 每次长期任务开始先拉取上下文，结束后更新记录、提交并推送。
- 无论项目内容多少，只同步可续接的精准压缩总结，不同步原始对话或全量内容。
- `AI学习`、`AI 教学助手`、`AI 笔记助手` 为设备 A 个人内容，双设备流程永久忽略；保留现有项，不删除。
- 设备 B 克隆后由根 `AGENTS.md` 自动理解并执行首次接入，不要求用户再次粘贴说明。
- 公共项目按 A、B 可同步项目的并集计算：任一端新增项目都要登记 Git 来源并补齐到另一端；任务仍只同步明确登记的长期任务。
- 用户主动发送“项目提交”“检查提交”“检查项目”或“项目检查”时，遍历当前项目列表下的全部 Git 仓库，先取得远端状态，再验证、提交、对齐并推送。
- 用户单独发送“开始”或“开始同步”时，核对两端最近发布的项目列表并补齐本机缺失项目；交班时写明当天工作、新增项目和另一端待办。

## 设备 A 已完成

1. 将上下文仓库克隆到：

   ```text
   ~/Desktop/DEV/Life/CodexSpace/CodexMissionContext
   ```

2. 将 `CodexMissionContext` 和 `lottery` 加入 Codex 左侧项目列表；保留原有 `AI学习` 与 ChatGPT 云项目 `22`。
3. 建立项目、任务、设备映射和“只增不减”清单。
4. 初始阶段曾导入 `AI 教学助手`、`AI 笔记助手` 的压缩上下文；用户随后明确将其改为设备 A 个人内容，退出共同清单。
5. 为 `lottery` 整理项目概览，但因当前项目中没有既有任务而没有伪造任务。
6. 把 `aidrama`、`skillnotes` 和全局 `AGENTS.md` 接入本仓库；原内容备份在 `~/.codex/sync-backups/20260728-163718-82334/`。
7. 建立本长期任务，设备 A 线程 ID：

   ```text
   019fa7e2-1ca2-7453-aadf-bbbb23d633f3
   ```

8. 创建 `main` 分支并完成首次远端推送。
9. 2026-07-30 将 `vid-mat-lab` 克隆到 `/Users/qichao/Desktop/DEV/Life/CodexSpace/vid-mat-lab`，验证 `main` 与 `origin/main` 同为 `cb82d20d8398dc7816a380a6674a036082e29e48`，并通过正式 Codex 深链登记为左侧本地项目；设备 A 本地 `projectId` 为 `8db92c0e-29dd-4e83-b042-9df1cb0344ae`。
10. 设备 A 已完整应用项目清单 `3`、任务清单 `3`；原有 `AI学习` 和 ChatGPT 云项目 `22` 均保留原样。

## 设备 B 已完成

1. 在 `D:\.aaProject-Bruce\CodexMissionContext` 启动首次任务，登记 `.device-id=B`。
2. 读取本机项目与任务列表，确认 `CodexMissionContext` 已存在，且保留全部额外项目和任务。
3. 从标准 Git 地址把 `lottery` 前、后端分别克隆到：

   ```text
   D:\.aaProject-Bruce\lottery\lottery-calculation-frontend
   D:\.aaProject-Bruce\lottery\lottery-calculation-backend
   ```

4. 在 `CodexMissionContext` 项目下创建长期任务“ 双设备同步维护 ”，本地线程 ID 为：

   ```text
   019fa8d5-32b4-7670-816e-b0dc862ee35c
   ```

5. 新线程已按规则仅确认“上下文已导入，等待继续”，没有擅自执行遗留工作。
6. 通过 Codex Windows 安装包提供的正式文件夹入口 `ChatGPT.exe --open-project`，把 `D:\.aaProject-Bruce\lottery` 登记为本地项目；没有使用界面自动化，也没有修改 Codex 数据库或配置。
7. 读取项目列表确认 D 盘项目显示名称为 `lottery`、路径精确为规定目录，本地 `projectId` 为：

   ```text
   71d16a40-79b9-4034-97b4-74d54bc0c500
   ```

8. 设备 B 已完整应用项目清单版本 `5`、任务清单版本 `3`，并回读当前 10 个 D 盘本地项目的真实 `projectId`。
9. `vid-mat-lab` 的设备 B 映射与远端代码均已补齐：本地 `projectId` 为 `local-d048fcddd193ac828b23b524d4434940`，代码已拉到 `697db9f`。

## 验证结果

- 三个 Shell 脚本通过 `sh -n` 语法检查。
- 仓库结构、敏感信息模式、嵌套 Git、大文件和 Git 差异检查通过。
- 两份仓库 Skill 与安装前原目录逐文件一致。
- 两份 Skill 的 YAML 元数据已按官方校验器同等规则解析并通过。
- 三个全局入口均已验证为指向本仓库的软链接。
- 安装脚本已通过幂等测试、错误设备标识拦截测试和注入失败后的自动回滚测试；回滚后原目录逐文件一致。
- 安装过程收到终止信号时会返回失败码，并继续完成全部回滚；隔离测试中原目录逐文件一致。
- 同步脚本已在隔离副本中验证：本地删除/重命名和远端删除/重命名都会退出并拒绝传播，原文件保持不变。
- 已手工提交但尚未推送的删除/重命名同样会被拒绝；普通推送失败留下的有效提交可在无新文件改动时自动重试。
- rebase 冲突会自动中止并恢复原 `main` 分支，本地提交仍保留，不会留下半完成 rebase。
- detached HEAD 会被拒绝；无 `rg` 环境会自动使用 `grep` 完成敏感信息与行尾检查，不再静默跳过。
- 设备 A 的 Codex 项目列表已确认包含 `CodexMissionContext`、`lottery`、`AI学习`；未删除任何已有项目。
- 长期任务初始化只导入上下文并等待，没有继续执行旧工作。
- 设备 B 已验证两个 `lottery` 远程仓库可访问，克隆后的分支与清单记录一致。
- 设备 B 已验证长期任务标题、项目归属、本地 `threadId` 和初始化回复。
- 设备 B 已验证 Codex 正式 `--open-project` 入口退出码为 `0`；随后项目列表返回 D 盘 `lottery` 的真实路径与 `projectId`。
- 设备 B 的 `lottery` 前、后端工作树均为 `master...origin/master`，没有本地修改。
- Windows 安装器已通过 `sh -n`；隔离 `CODEX_HOME` 首次安装和二次幂等运行均成功。
- 设备 B 实机已确认 `aidrama`、`skillnotes` 的 `LinkType` 为 `Junction`，物理目标均解析到 D 盘仓库；Windows 全局入口与仓库引导文件 SHA-256 一致。
- 设备 B 仓库检查通过；首次接入主提交 `256071b` 已推送到 `origin/main`，本地与远端引用一致。
- 设备 A 的 `vid-mat-lab` 克隆完成后通过 `git fsck --connectivity-only`；仓库总计约 `696 MB`，第 010 集脚本、提示词、剪辑和发布文件存在，不是空壳项目。
- 设备 A 再次读取 `origin/main`，确认远端仍为 `cb82d20d8398dc7816a380a6674a036082e29e48`；永久原版底稿 SHA-256 为 `C5331DA5F48B986AFE738123E5C876B0EA7F6BBD5CF0336009732CD2CCD1DC0B`，与项目概览一致。
- 设备 A 的 Codex 项目列表已回读确认包含 `CodexMissionContext`、`lottery`、`vid-mat-lab` 三个共同项目；`vid-mat-lab` 路径与 `projectId` 已写入设备映射。
- 设备 A 的 `lottery` 前、后端均执行 `git pull --ff-only`，结果为 `Already up to date`；两个工作树分别为 `master...origin/master` 且无本地修改。

## 已遇到的问题

标准 `github.com` SSH 入口在设备 A 上认证为 `wajjforever1314`，GitHub 因该账号邮箱未验证拒绝推送。设备 A 改用现有 SSH 别名 `github-cloudystormer` 后，认证为 `CloudyStormer` 并成功推送。仓库文档仍保留标准克隆地址，设备 B 使用自己的有效身份。

官方 Skill 快速校验脚本在本机缺少 `PyYAML`，因此未直接运行成功；随后使用本机 YAML 解析器按该脚本的同等字段、命名和长度规则校验，两份 Skill 均通过。

2026-07-28 用户将 `AI学习` 明确改为设备 A 个人范围。另一条正在运行的 AI 学习任务在新排除规则完成前误把部分改动提交为 `7454876`；该并发提交被保留，没有重写历史。随后完成两层隔离：

1. 全局规则要求位于 `/Users/qichao/Documents/AI学习/**` 时立即停止本仓库全部同步行为。
2. 同步脚本从该目录调用时直接成功跳过，隔离测试已通过。

同步仓库中的三份 AI 学习摘要副本和本地 ID 已按用户授权移除；设备 A 的真实项目目录、左侧项目和原任务均未删除、归档、重命名或隐藏。

设备 B 首次运行旧安装器时，Git Bash 的 `ln -s` 在 Windows 上静默退化为普通复制。内容虽然一致，但不会跟随仓库更新，且旧回滚逻辑可能无法清理该复制。修正后：

1. Windows Skill 目录使用无需管理员权限的 Junction，并在安装后验证 `LinkType` 和物理目标。
2. `C:\Users\Administrator\.codex\AGENTS.md` 使用稳定引导文件，每次指向 D 盘仓库中的权威规则，避免复制规则正文。
3. 非 Windows 仍使用符号链接；安装变更日志区分 Junction、符号链接和引导文件，失败时按类型回滚。
4. 旧版生成的普通副本没有直接删除，已备份到 `C:\Users\Administrator\.codex\sync-backups\20260728-211650-739\` 后再完成修复。

首次执行结束同步时，Git 因设备 B 尚未配置作者身份而在创建提交前退出；文件只被暂存，没有生成提交或推送。随后读取本仓库既有提交作者，仅在当前仓库的 `.git/config` 写入同一作者身份，没有修改全局 Git 配置；再次执行后提交和推送成功。

首次添加 `lottery` 时曾观察到 `C:\Users\Administrator\Documents\lottery` 同名项目，不符合共同项目必须位于 D 盘的约束。后续改用 Codex 正式 `--open-project` 文件夹入口登记 `D:\.aaProject-Bruce\lottery`；最终复核项目列表时已不再返回该 C 盘项目。本同步流程未调用删除、归档、重命名或隐藏接口，不把其消失归因于本流程，并以 D 盘项目列表返回的真实路径作为完成证据。

2026-07-29，正式登记项目后，本任务和独立子代理的 `apply_patch` 均报 `windows sandbox failed: helper_unknown_error: setup refresh had errors`。先用 `dos2unix` 仅统一目标 Markdown 的行尾，再通过 `git apply --check` 和 UTF-8 标准输入校验后应用同一最小补丁；没有使用覆盖式写文件。

同日，其他临时诊断任务在共享工作树执行结束同步时，分别把本任务当时已完成的文件变化纳入 `5951ecf` 和 `d04cb91`，导致提交标题与实际变更范围不一致。两个提交均已推送并保留，没有重写历史；临时诊断内容仍未写入共同任务清单，本记录只保留该并发事实与处理边界。

2026-07-30，设备 A 尝试向设备 B 已记录的维护线程 `019fa8d5-32b4-7670-816e-b0dc862ee35c` 发送续接指令，Codex 返回 `No Codex thread found`。当前 A 端没有可用的 B 主机连接，因此不能从 A 远程读取或修改 B 的本地项目列表；必须等设备 B 下次打开任务并执行开始同步后完成本地回读，不能把远端上下文已推送误报为 B 本地验证完成。

## 2026-07-30 设备 A 交班同步

用户要求把当天本机所有任务、相关项目仓库内容和本维护记录全部推送，以便设备 B 接班继续。

### 本机任务盘点

1. `双设备同步维护`（本线程）：长期共同任务，负责本次完整盘点和交班推送。
2. `第010集拼豆图纸与水印`：线程 `019fb114-130d-7e50-aacd-f6a26b403b91`，工作目录为设备 A 的 `vid-mat-lab`。当天三次生产提交均已推送；最新水印请求未生成文件，已建立独立交班记录。
3. `业界前五 AI 快速说明`：线程 `019fa252-8dee-7502-b8b1-2c9e97575607`，无代码仓库；启动资料核对后发生 `systemError`，没有最终答案，已建立独立未完成记录。
4. `AI学习` 项目中的任务仍按用户既定要求永久排除，没有读取、修改、登记或推送。

### Git 仓库盘点

设备 A 的 `CodexSpace` 下共发现四个 Git 仓库。交班前均执行 `fetch --prune` 与 `pull --ff-only`：

| 仓库 | 分支 | 交班状态 |
|---|---|---|
| `CodexMissionContext` | `main` | 交班记录写入前为 `7e27d99`，无原有未提交或未推送内容 |
| `vid-mat-lab` | `main` | 生产内容和交班说明已推送至 `697db9f`，工作树干净 |
| `lottery-calculation-frontend` | `master` | `ea3f326`，当天无提交，远端最新 |
| `lottery-calculation-backend` | `master` | `e2675e5`，当天无提交，远端最新 |

当天 `vid-mat-lab` 的生产提交为：

- `1ad8847`：80×80 高还原拼豆模板；
- `e2b1cdc`：Artkal 官方色号版；
- `4ec7eb7`：按 MARD 221 重做 120×120 当前生产版；
- `697db9f`：补充 Pillow 依赖、可重建验证和水印分享版交班状态。

对应上下文提交 `b00685f`、`51f9423`、`7e27d99` 已在此前推送；本次再补齐独立任务记录和最终交班状态。

### `aidrama` 差异

设备 A 当前只有由本仓库管理的全局 `aidrama` Skill，`~/.codex/skills/aidrama` 指向 `全局技能/aidrama`，`CodexSpace` 下不存在独立 `aidrama` Git 仓库。用户确认另一台 Codex 中存在名为 `aidrama` 的项目，并把公共项目重新定义为两端项目并集，因此该项目已加入项目清单版本 `4`。设备 B 必须回读路径、`projectId`、组成仓库和 Git 来源；在事实确认前，不把 Skill 目录冒充项目，也不声称设备 A 已补齐。

## 当前阻塞

设备 A 已完整应用任务清单 `3` 和项目清单 `3`；目标项目清单已升至 `4`，新增的 `aidrama` 项目因设备 B 尚未提供真实 Git 来源而阻塞。

设备 B 的 `vid-mat-lab` 仓库和 D 盘路径已经就绪，但本地 `projectId` 没有进入持久化映射；`aidrama` 项目也缺少真实路径、`projectId` 和 Git 来源。当前设备 A 无法读取另一台机器的本地状态。设备 B 下次“开始同步”时必须回读并发布两项信息，使设备 A 能补齐并集版本 `4`。

`AI学习` 已退出共同范围，不再是阻塞项，也不再进行 Git 来源检查。

## 后续整改顺序

1. 设备 B 拉取本次记录并安装 `check-projects`，回读并补记 `vid-mat-lab` 的真实本地 `projectId`。
2. 设备 B 回读并集项目 `aidrama` 的真实路径、`projectId`、组成仓库和 Git 来源；保留原项，不猜测与 Skill 的关系。
3. 设备 A 根据 B 发布的真实 Git 来源克隆并加入 `aidrama`；两端核对 `CodexMissionContext`、`lottery`、`vid-mat-lab`、`aidrama` 四个并集项目。
4. 设备 B 读取第 010 集和“业界前五 AI”两份独立交班记录，按各自精确下一步继续。
5. 两端完成本地映射后分别把项目清单应用版本更新为 `4`，运行检查并结束同步。
6. 以后主动“开始同步”时补齐并集，“项目提交”时走查并推送全部项目，“交接”时告知另一端当天工作与待办。

## 2026-07-30 主动项目协作机制升级

用户明确纠正：全项目巡检不是每次普通任务结束自动执行，而是使用固定口令主动触发；同时将“公共项目”定义为 A、B 项目列表的并集，而不是交集或预先固定的小集合。

本次新增第三个个人 Skill `check-projects`，显示名“检查项目”：

- “检查提交”“检查项目”“项目检查”“项目提交”：先 fetch/取得远端最新状态，再遍历当前设备项目列表中的全部 Git 仓库，审查、验证、提交、对齐并推送。
- “开始”“开始同步”：拉取上下文，发布本机新增项目，读取另一端已发布映射，以并集为目标克隆并加入本机缺失项目。
- “交接”“晚间交班”：先执行项目提交，再记录当天项目、提交、新增项、阻塞和对方精确待办；上下文推送成功后尝试通知另一端维护任务。

项目并集采用只增不减实现一致。`AI学习` 继续遵守既有最高优先级排除，除非用户以后明确点名解除；临时任务和聊天仍不进入项目同步范围。另一台设备离线时，本机只能使用它最后推送的映射，不能发现其尚未提交的本地文件。

项目清单已升至版本 `4`，并把设备 B 已存在的 `aidrama` 项目正式登记为并集待补齐项。当前唯一来源阻塞是设备 B 尚未发布该项目的真实路径、`projectId`、组成仓库和 clone 地址。

### 实现与验证

- Skill 位于 `全局技能/check-projects/`，包含标准 `SKILL.md`、`agents/openai.yaml` 和只读盘点脚本 `scripts/inspect-project-repos.sh`。
- 官方 `quick_validate.py` 已通过；`openai.yaml` 由官方生成脚本生成。
- 盘点脚本通过 macOS 自带 Bash 3.2 语法与实机测试，成功发现当前三个本地项目中的四个 Git 仓库：`CodexMissionContext`、`vid-mat-lab`、`lottery` 前端和后端。
- 正式项目列表接口再次确认设备 A 当前包含 `CodexMissionContext`、`vid-mat-lab`、`lottery`、明确排除的 `AI学习` 和无需本地重建的云项目 `22`；没有遗漏其他 A 端可同步项目。
- 发布前实机盘点显示三个业务仓库均为干净且与 upstream 一致；上下文仓库当时只包含本次 Skill、并集规则和交接记录修改。
- 两个独立只读前向测试分别用“开始同步”和“项目提交”触发 Skill：均正确识别运行模式、先处理三个业务仓库并把上下文仓库放在最后，也正确把缺少 B 端 Git 来源的 `aidrama` 标记为阻塞；测试未修改文件或 Git 状态。
- 设备 A 已运行 `脚本/安装到本机.sh A`，确认 `~/.codex/skills/check-projects` 指向本仓库 Skill；设备 B 必须拉取后运行 `脚本/安装到本机.sh B`，再重启 Codex 或新建任务。
- Skill、项目清单版本 `4` 和并集规则已由设备 A 提交并推送到 `origin/main`，功能发布提交为 `45d62cb`。
- 设备 A 在上下文更新至 `0f6588b` 后，向设备 B 已登记维护线程 `019fa8d5-32b4-7670-816e-b0dc862ee35c` 发送安装和项目补齐通知；Codex 再次返回 `No Codex thread found`。设备 B 当前不可由 A 主机直接通知，远端上下文与本记录是其下次“开始同步”时必须读取的权威交接。


## 2026-07-30 设备 B 完整同步与四字口令

用户连续发送“开始同步”，并进一步确认：四字口令“开始同步”必须保证当前 Codex 能访问的本地与线上任务、两端项目清单和项目远端代码都尽可能完整拉下；不能只做普通任务开始或轻量日结。

### 已执行

1. 设备 B 先后拉取设备 A 当天 9 个上下文提交；两次 rebase 冲突均以远端新事实为底稿，合并本机已确认映射和同步规则，没有覆盖任一端记录。
2. 正式运行 `脚本/同步上下文.sh 开始`，脚本确认设备 B 已取得上下文最新版本。
3. 使用 Codex 项目与任务列表能力刷新当前可见状态：返回 10 个 D 盘本地项目、1 个账号级云项目和 31 个可见任务（含 1 个置顶任务）；`unavailableHosts`、`unavailableSources` 均为空。
4. 使用 `check-projects/scripts/inspect-project-repos.sh --fetch` 盘点 9 个业务项目，共识别 21 个有效 Git 仓库；项目清单由版本 `4` 升至 `5`，写入设备 B 真实 `projectId`、组成仓库和 clone 地址。
5. `vid-mat-lab` 从 `cb82d20` 快进至设备 A 交班提交 `697db9f`，本机原有未确认改动通过 autostash 完整恢复，最终 `ahead=0 / behind=0`。
6. 更新 `check-projects`：四字口令“开始同步”现在明确执行上下文、任务、项目并集和全部仓库的完整同步；`daily-project-sync` 只处理普通每日汇总，避免触发冲突。
7. 尝试在设备 B 重新运行安装脚本；`daily-project-sync` Junction 已存在，但当前沙箱拒绝写入 `C:\Users\Administrator\.codex\skills`，`check-projects` 安装自动回滚。仓库内 Skill 已完整，待具备本机配置写权限时重试。

### 项目仓库结果

- `Aidrama`、`vid-mat-lab`、三个 liquidity 项目的六个仓库、`canvas-garment`、`lottery` 前后端及多数 `funhub-space` 仓库均已取得远端状态；除下列阻塞外均为 `ahead=0 / behind=0` 或保持原任务未确认工作树。
- `Demo` 前后端没有可解析 HEAD/upstream，且分别存在 215、98 项未确认初始内容；不由同步任务首次提交。
- `aime-bridge-backend` 为 `behind=1` 且有 19 项未确认内容；尝试创建保护 stash 时因当前任务没有目录写权限而失败，仓库现场未改变。
- `english-talk-trainer` 与 `MountainFruitCottage` fetch 失败。
- `funhub` 当前分支有 2 个既有本地提交；push 先因 Windows HTTPS 凭证缺失返回 `SEC_E_NO_CREDENTIALS`；随后使用 SSH 地址重试又因当前沙箱拒绝读取 `C:\Users\Administrator\.ssh\known_hosts` 失败，未能上传。

### 可见性边界

“完整同步”只对当前工具实际可访问的主机和账号来源作保证。当前任务列表没有报告不可用来源，但另一台离线设备尚未提交到 Git 的本地文件和本地任务状态仍无法远程读取；这部分必须由该设备下次执行“开始同步”后发布，不能假装已拉取。

### 后续

1. 设备 A 拉取项目清单版本 `5`，补齐 B 发布的 7 个新增项目并回写映射。
2. 在具备项目目录权限的任务中安全拉取 `aime-bridge-backend`；修复 GitHub HTTPS 凭证后推送 `funhub` 的 2 个提交，并重试两个 fetch 失败仓库。
3. `Demo` 由原业务任务完成首次确认提交后再补齐另一端。


完整同步记录已成功推送，设备 B 的 `HEAD` 与 `origin/main` 一致。

最终全项目 fetch 复核再次运行时被私有仓库连接卡住并在 120 秒超时；本次命令遗留的 Git/Bash 子进程已全部停止，没有修改工作树。

## 2026-07-31 设备 A 完整同步与侧边栏布局

用户在设备 A 再次发送“开始同步”，并追加新要求：四字口令以后不仅同步任务、项目并集和仓库，还要把当前设备共享侧边栏中的项目置顶、项目顺序和共同长期任务置顶发布给另一端；另一端必须按发布顺序应用。

### 上下文与身份修复

1. 第一次开始同步因本机旧 SSH 别名 `github-cloudystormer` 已不存在而失败。
2. 已验证 `github.com-cloudystormer` 认证为 `CloudyStormer`，只修改本机 Git remote 后重新执行开始同步，成功快进到设备 B 提交 `f4a6921`。
3. 同样修复 lottery 前后端和 vid-mat-lab 的本地 remote；标准共享克隆地址仍使用 `git@github.com:CloudyStormer/...`。
4. 设备 B 新增的 `daily-project-sync` 已通过 `脚本/安装到本机.sh A` 接入，四个个人 Skill 均由本仓库链接管理。

### 当天任务元数据

- 正式任务列表最新返回 22 个可见任务，`unavailableHosts=[]`、`unavailableSources=[]`。
- `AI工作流（试点）` 当前有 11 个当天任务：活动中的“包工头”、状态为 `systemError` 的“项目经理”，以及已空闲的 UI/UX设计师、产品经理、架构师、前端工程师、后端工程师、数据工程师、测试工程师、DevOps工程师、代码审查员。
- 这些任务均为普通本地任务，用户没有指定为双设备共同长期任务；只同步标题、项目和状态等元数据，不复制原始对话，也不加入共同任务清单。
- 当前“ 双设备同步维护 ”仍为共同长期任务；设备 B 已发布的“差评死神”待 Aidrama 完成后在 A 端补建。

### 项目并集

1. 设备 A 正式项目列表除既有项目外，还发现 `AI工作流（试点）`，`projectId=d43e856c-a6e4-4ab5-8857-041fd346b853`；它已进入项目清单版本 `6`。
2. 该项目根目录 `/Users/qichao/Desktop/DEV/Lenovo/AIWorkFlow` 不是 Git 仓库，只有 `projects/ai-english-learning` 可由 `git@github.com:CloudyStormer/AI-Workflow-Dev.git` 取得；B 端根目录来源仍阻塞。
3. 设备 A 已克隆并正式登记 canvas-garment、funhub-space、liquidity-bmi、liquidity-portrait、liquidity-watermark-assistant，真实 `projectId` 已写入设备映射。
4. funhub-space 已取得 6 个干净且与 upstream 一致的仓库；`english-talk-trainer` 与 `MountainFruitCottage` 以 `CloudyStormer` 身份读取时返回 `Repository not found`，没有创建假目录。
5. Demo 两个远端存在但没有任何 HEAD 引用；不创建空壳。Aidrama 远端约 1.5 GB，本次坚持普通完整 clone，不改 shallow 或 partial。

### 仓库结果

- lottery 前后端分别为 `e2675e5a0884`、`ea3f326016d0`，干净且与 upstream 一致。
- vid-mat-lab 已快进到设备 B 的 `c9f5919c5fc2`，干净且与 upstream 一致。
- canvas-garment、六个可访问 funhub 仓库和三个 liquidity 项目的六个仓库均为 `ahead=0 / behind=0` 且工作树干净。
- `AI-Workflow-Dev` 的远端仍为 `da677f268ead`；本次同步期间其他任务创建本地提交 `8dd9822adddc`（领先远端 1 个），并保留 11 个未提交 Skill 文件修改。本任务未 stash、未暂存、未提交、未推送。
- “开始同步”模式已被收紧为只 fetch，并只对干净且可 fast-forward 的业务仓库拉取；业务脏工作树只保留并报告。完整提交流程只属于“项目提交”等明确模式。

### 侧边栏布局版本 1

1. `check-projects`、全局规则、仓库规则、README、首次接入指令和增量对齐规则已加入布局同步。
2. 共享布局只保存逻辑 ID。设备 A 当前依次置顶 `mission-context`、`ai-workflow`；`AI学习` 虽在 A 本地置顶，但永久排除，不传到 B。
3. 非置顶共享项目顺序和共同长期任务置顶状态保存在 `同步清单/侧边栏布局.md`；当前两项共同长期任务均非置顶。
4. 另一端落后时必须先应用远端版本，不能用旧本地顺序覆盖。应用采用稳定合并，只重排共享对象子序列；个人/临时对象的置顶状态和相对顺序保持不变。
5. 部分应用分别记录“已完整应用版本”“部分应用目标版本”“未完成逻辑 ID”；缺少对象或正式能力时不提升完整版本，也不发布残缺新版本。
6. 只允许正式 API 或用户可见界面应用布局；可以只读提取 `project-order`、`pinned-project-ids`、`pinned-thread-ids` 做逻辑映射，但禁止复制、提交或写回 Codex 原始状态。
7. 更新后的 Skill 已通过等价 YAML/字段校验和两轮独立只读前向测试；第二轮未发现剩余矛盾或不安全歧义。

### 并发提交事实

AI工作流任务在本任务修改共享工作树期间两次运行结束同步，把部分本任务变化分别纳入 `cb51910` 和 `50c9adc` 后推送，提交标题与实际文件范围不一致。历史已经远端发布，保留且不改写；本任务已向线程 `019fb738-5706-7552-849f-35c8a124e2f0` 发送协调消息，请其暂时不要再提交 CodexMissionContext 的未提交变化。

### 当时待完成（已于 2026-08-01 完成或复核）

1. 等待 Aidrama 完整 clone，验证后通过正式入口登记。
2. 在 Aidrama 项目下补建 A 端共同长期任务“差评死神”，只导入上下文并等待。
3. 更新 A 项目/任务映射和布局部分应用状态。
4. 复核全部仓库、运行上下文检查并完成最终推送。

## 2026-08-01 设备 A 完成 Aidrama、共同任务与布局版本 2

### Aidrama 完整接入

1. 普通 SSH clone 长时间低速；HTTPS 普通 clone 在约 420 MiB 因 HTTP/2 连接取消而失败，均未被误报为完成。
2. 使用 Git partial 元数据取得 13 个提交、929 个可达对象目录，再把 770 个缺失 blob 映射到真实提交路径；通过 8 路 raw CDN 下载逐对象校验 Git OID，最终 `missing=0`、失败 0，并移除 partial/promisor 状态。
3. 本机原先没有 Git LFS；旧 Homebrew 因无法识别当前 macOS 而失败。随后下载官方 Git LFS v3.7.1 arm64 包，核对发布页 SHA-256 `76260fb34f4ee622ff0a66b857e5954aa49c7e343a92e57a1ec4a760618c94b2` 后安装到 `/opt/homebrew/bin/git-lfs`。
4. Git 全历史严格 fsck 通过；LFS 全历史 60 个对象、`967,655,248` 字节全部 fetch/checkout，逐对象 SHA-256、大小和 `git lfs fsck` 均通过。正式仓库约 `4,811,079,680` 字节，无子模块。
5. 正式路径为 `/Users/qichao/Desktop/DEV/Life/CodexSpace/Aidrama`；`main` / `3baedae5d3bb` 跟踪 `origin/main`，ahead/behind `0/0`，工作树干净。
6. 通过正式 Codex 入口登记项目，`projectId=0e940644-86a0-4fd1-83e4-1813e660d45b`。

### 共同任务

- A 端“差评死神”线程为 `019fb94b-0199-7c32-8a8c-e521082eb33f`，保持非置顶。
- 初始化只读取项目概览和任务记录，最终回复“上下文已导入，等待继续。”；没有执行遗留工作，也没有在6集现实悬疑版与8集赛博爽剧早期版之间选择。
- 任务清单版本 `4` 已在设备 A 完整应用。

### 侧边栏布局版本 2

- 本机共享置顶子序列变为 `mission-context`、`ai-workflow`、`vid-mat-lab`；个人 `AI学习` 继续排除。
- 非置顶共享子序列为 `aidrama-project`、三个 liquidity 项目、`canvas-garment`、`funhub-space`、`lottery`、`demo`；Demo 沿用逻辑末位。
- 设备 A 完整应用版本仍为 `0`、已部分应用版本 `2`、未完成仅 `demo`；设备 B 最近自行确认的完整/已部分应用版本均为 `0`。A 只登记“待 B 获取版本 `2`”及预期阻塞 `ai-workflow`，不替 B 预报收件或应用结果。
- 协议补充严格例外：发布设备缺少并集对象时，仅当缺失对象沿用上一版本位置、可见变化明确且没有同基线冲突，才可发布下一版本；不得用部分视图移动缺失对象。
- 发布流程采用候选先行的两阶段协议：版本 `2` 候选已先以 `902e208` 推送；再次 fetch 确认规范仍为版本 `1` 且候选唯一后，以 `13187ed` 晋升有效版本。设备应用状态、目标分发状态和并发候选分别记账。
- `check-projects` 已通过独立前向场景复核；官方 Python 快速校验因环境缺少 `yaml` 模块未能启动，Ruby YAML 按同一 frontmatter 与 `openai.yaml` 约束校验通过。

### 同步结果与剩余阻塞

- 项目清单版本 `7`、任务清单版本 `4`、布局版本 `2` 已更新；Aidrama 项目与任务映射已回写。
- `vid-mat-lab` 已干净快进到设备 B 的 `6b65ffe`。
- 最终复核 20 个可访问业务仓库全部干净；18 个远端仓库 fetch 成功且 ahead/behind 均为 `0/0`，AI Model Radar `a32da87` 与 Control Center `da600ca` 无普通 remote。
- 正式任务列表共 24 项，当前没有不可达主机或来源；12 个 AIWorkflow 固定角色任务仍不是共同长期任务。
- 剩余阻塞为 Demo 无 HEAD、两个 funhub 来源返回 `Repository not found`、设备 B 的 funhub 本地提交不可见，以及 AIWorkflow 根框架/雷达/控制中心无普通 Git 来源。
- 尝试向设备 B 的维护线程 `019fa8d5-32b4-7670-816e-b0dc862ee35c` 发送交接通知时，当前 Codex 返回 `No Codex thread found`；未声称送达，以远端上下文作为下次接班入口。

## 2026-08-01 设备 B 取得布局版本 2 并完整复核

用户在设备 B 发送四字口令“开始同步”。本机运行 Windows 开始同步后，上下文仓库从 `29f43b8` 快进到设备 A 发布的 `d26a0a8`，正式取得项目清单版本 `7`、任务清单版本 `4` 和有效侧边栏布局版本 `2`。

### 项目、任务与布局

- 正式项目列表返回 10 个位于 `D:\.aaProject-Bruce\` 的本地共同项目和账号级云项目 `22`；没有发现新的可同步本地项目。
- 设备 B 仍缺 `ai-workflow`。已发布来源只有英语子仓库，根框架、雷达和控制中心没有普通 Git 来源，因此继续阻塞，不创建不完整目录冒充完整项目。
- 正式任务列表返回当前本机与账号可见任务，`unavailableHosts=[]`、`unavailableSources=[]`；没有置顶任务，因此“双设备同步维护”和“差评死神”均符合布局版本 `2` 的非置顶要求。另一台离线设备未推送的本地任务与文件仍不可见。
- 本会话没有正式项目排序或项目置顶 API；Windows Computer Use 的强制安全规则禁止自动化 Codex/ChatGPT 桌面应用，因此没有用界面拖动或直接写本地状态。布局版本 `2` 实际记为部分应用：共同任务置顶已验证，项目排序/置顶和缺失的 `ai-workflow` 尚未完成。

### 业务仓库只读同步

- 共发现 22 个有效 Git 仓库；`liquidity-portrait` 和 `liquidity-watermark-assistant` 根目录只是多仓项目容器，不是额外 Git 仓库。完整同步模式只 fetch，并只允许干净且可 fast-forward 时拉取；本轮没有 stash、暂存、提交或推送业务仓库。
- Aidrama、canvas-garment、CodexMissionContext、liquidity-portrait 前后端、liquidity-watermark-assistant 前后端、lottery 前后端和 vid-mat-lab 成功 fetch。干净仓库均无落后提交；脏工作树全部保留现场。
- `Demo` 前后端仍无 HEAD/upstream，并保留未确认初始内容。
- `aime-bridge-backend` 的既有远端引用显示落后 1 个提交，但工作树有 19 项改动，且本轮 HTTPS fetch 失败；未拉取。`funhub` 工作树干净但仍领先已知 upstream 2 个提交；“开始同步”不授权推送，故保留。
- funhub-space 的 8 个 HTTPS 仓库与 liquidity-bmi 前后端本轮均因无法连接 GitHub 443 而 fetch 失败；其他独立仓库继续完成检查。整批官方盘点脚本先因慢远端在 124 秒超时，随后改为逐仓库限时复核并得到上述结果。

### 当前精确状态

- 项目清单：目标版本 `7` 已取得，设备 B 仍因 `ai-workflow` 来源缺失而保持已应用版本 `5`。
- 任务清单：版本 `4` 已完整应用。
- 侧边栏布局：有效版本 `2` 已取得并实际尝试，设备 B 为已部分应用版本 `2`、已完整应用版本 `0`。
- 仍需重试：补全 `ai-workflow` 可传递来源；在正式项目排序/置顶能力可用时应用项目布局；网络恢复后重新 fetch 10 个本轮 HTTPS 超时仓库；由原业务任务处理各脏工作树、`aime-bridge-backend` 落后提交、`funhub` 两个未推送提交和 Demo 首次提交。
