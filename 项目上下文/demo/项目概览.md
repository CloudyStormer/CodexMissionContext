# 项目：演示 - 多垂直赛道（尚未收敛）

最近更新：2026-07-31

## 一句话说明

由课程演示前端和后端两个仓库组成的项目。

## 仓库与映射

- 设备 B 根目录：`D:\.aaProject-Bruce\Demo`，`projectId=local-81d8c01b7837e883f7b7ea9a01f3a3dc`。
- 前端 remote：`git@github.com:CloudyStormer/demo-for-course-frontend.git`。
- 后端 remote：`git@github.com:CloudyStormer/demo-for-course-backend.git`。

## 当前阻塞

设备 A 再次以 `CloudyStormer` 身份读取两个远端：远端存在，但 `HEAD` 均没有返回任何引用。设备 B 两个本地仓库均在 `main`，没有可解析 HEAD 和 upstream，且存在尚未确认的初始工作；不得由同步任务代为首次提交。设备 A 在远端产生可克隆提交前不能把空壳误报为补齐。
