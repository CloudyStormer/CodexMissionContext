# 任务：每日实盘、近月回测与预测账本

- 日期：2026-08-17
- 范围：前端设计任务书、现有项目本地启动、官方数据刷新检查；业务代码尚未进入开发
- 用户目标：每期生成并保存号码，开奖后对比实际结果，统计长期命中表现；从 2026-07-17 到 2026-08-17 做无未来数据的历史模拟，并让后续算法升级可追溯

## 本轮完成

1. 按项目规则同步上下文；前、后端分别 fetch，开始时均与 upstream 一致且工作树干净。
2. 重建后端 `.venv`；未触碰端口 8000 上的独立项目。lottery 当前本地运行地址为前端 `http://localhost:5173`、后端 `http://127.0.0.1:8001`。
3. 官方数据刷新结果：
   - 福彩：双色球更新到 `2026094`；福彩3D、快乐8更新到 `2026218`；七乐彩更新到 `2026093`；同步时间为 2026-08-17。
   - 体彩：四个玩法的官方接口触发访问保护，刷新失败；旧缓存不得冒充最新数据。
4. 按 UI/UX 门禁交付《每日实盘与滚动回测设计提示词与设计任务书 v1》，设计关键口径包括：
   - 每期主方案只生成并锁定一次，防止反复重抽后挑号；
   - 历史回测使用逐期 walk-forward，只能读取当期开奖前数据；
   - 历史模拟和真实预先生成记录强制区分；
   - 同期展示等概率基线、置信区间和样本量；
   - 实验模型没有通过时间外验证时，主方案回退到严格等概率、跨注低重叠覆盖；
   - 算法升级版本化，不覆盖旧预测或旧回测。

## 产物与 Git

- 前端开始提交：`ea3f326016d0c34ed4bf7ba2b612feb65a0747e4`
- 前端结束提交：`5375e51`（已推送 `origin/master`）
- 前端设计任务书：`ui/2026-08-17-daily-lottery-lab-design-brief-v1.md`
- 前端产物登记：`workflow/artifacts.yaml`
- 后端开始/结束提交：`e2675e5a0884513f989b5e9d86bab0a5b1f1d36d`，业务代码无修改
- 设计任务书 SHA-256：`3e3b928fedb63dd9b36e6d9358934063ca9ce0b8e663d4d2f2362507633ff743`

## 验证

- `workflow/artifacts.yaml` 可被 YAML 解析。
- 设计任务书登记 SHA 与实际文件一致。
- 后端 `/health` 返回 `{"status":"ok"}`。
- 前端根页面返回 HTTP 200。
- 前端提交已经推送，工作树干净且与 upstream 一致。

## 当前门与下一步

- 当前停在 UI/UX《设计提示词与设计任务书》审核门。
- 用户明确回复“通过”后，唯一下一站是 UI/UX 详细页面设计与交互规格；详细设计完成后仍须再次审核，尚未授权前后端业务代码开发。
- 后续开发必须先解决或显式阻断体彩官方数据刷新失败，不能用过期缓存生成今日主方案。

## 同日后端故障修复

- 用户在当前页面选择 7 星彩抽样时看到“本次未能完成 / Failed to fetch”。
- 真实根因不是前端网络：后端仍按旧规则把 7 个位置全部限定为 `0—9`，但现行 7 星彩最后一位为 `0—14`；本地官方历史中出现 `12` 后触发 NumPy 数组越界，接口返回 500。
- 已按体彩官方规则把号码空间修正为前六位 `0—9`、最后一位 `0—14`，一等奖理论单注空间修正为 `10^6 × 15 = 15,000,000`。
- 新增目录规则和真实后区范围回归测试；后端 `pytest` 为 `8 passed`，`ruff check .` 通过。
- 浏览器已实际重载并点击 7 星彩“开始等概率抽样”，页面成功显示三组号码和随机性审计，不再出现失败提示。
- 后端修复提交：`3b9414d`，已推送 `origin/master`。
- 本修复只处理当前抽样故障。历史模拟准确率页面仍未实现，因为 UI 设计任务书仍在待审核状态；不得把当前“历史随机性审计”冒充为回测命中率。

## 方法边界

- 公平彩票的历史开奖不能保证提高下一期合法单注的理论中奖概率。
- 可以科学增强的是：数据质量、无泄漏回测、组合覆盖、实验对照、记录不可篡改和模型准入纪律；不能把随机波动包装成可承诺的盈利能力。

## 同日产品变更：7 星彩三注最大覆盖

- 用户进一步要求“升级成更高概率的中奖算法”。该指令会改变生产选号目标，因此旧 UI 提示词已冻结并回退到产品审核门，尚未修改业务代码。
- 产品主指标被严格定义为：固定三注时“至少一注获得任意奖级”的理论概率；不把它误写成一等奖概率或盈利概率。
- 按体彩现行奖级规则：
  - 单注任意奖概率为 `1,221,900 / 15,000,000 = 8.146%`；
  - 当前随机三注至少一注中奖约 `22.50133656%`；
  - 三注在每个对应位置均取两两不同值时达到全局最优覆盖 `3,569,880 / 15,000,000 = 23.7992%`；
  - 相对当前基线绝对增加 `1.29786344` 个百分点、相对提升 `5.76794%`；
  - 三注一等奖概率仍为 `1 / 5,000,000`，单注边际概率、期望中奖注数和期望回报不变。
- 覆盖升级无需历史数据即可严格证明；历史频率、遗漏、共现等保持实验隔离，必须与 `23.7992%` 强基线做无未来数据时间外检验，证据不足自动回退。
- 数据审查确认本地 7 星彩最新为 `26077 / 2026-07-07`，`2026-07-17—2026-08-17` 当前为 0 条，不能展示伪造的近月准确率；旧、新规则历史也必须分段。
- 已交付 PRD：前端仓 `docs/product/2026-08-17-qxc-max-coverage-prd-v1.md`；SHA-256 `e2f7ca581b5f2a7e202232079ad1561b4ad84e700d564576ebe63ebf159b7ebc`。
- 前端文档提交：`7b23f42`，已推送 `origin/master`。
- 当前停止门：PRD 待用户明确“通过”；通过后唯一下一站为 UI/UX 提示词修订，不直接授权开发。

## 同日审核进展：PRD 已通过，UI 提示词待审

- 用户已明确回复“通过”，`lottery-qxc-max-coverage-20260817-v1` 产品 PRD 状态更新为 `approved`。
- 已依获批 PRD 交付 `ui/2026-08-17-qxc-max-coverage-design-brief-v2.md`，覆盖：旧算法禁用态、最大覆盖版本标识、三注七列互异校验、生成并锁定、理论概率边界和真实账本状态。
- UI 提示词 SHA-256：`d2ddb22bf4281f28e13bbba6f508814a9720e058a6ac26f1762c4b12e4d07642`。
- 前端文档提交：`d81beb2`，已推送 `origin/master`。
- 当前页面的“开始等概率抽样”仍调用旧版普通随机三注，只保证整注不重复，没有强制七个对应位置三值互异；因此可作普通等概率样本，但不得标记为 `23.7992%` 最大覆盖方案。
- 当前停在 UI/UX 设计提示词审核门。用户再次明确“通过”后，唯一下一站是详细页面设计、组件规格和交互原型；仍不直接授权前后端开发。
