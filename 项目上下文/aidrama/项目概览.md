# 项目：aidrama

最近更新：2026-07-30

## 一句话说明

用户确认存在于设备 B Codex 项目列表、需要按两端项目并集补齐到设备 A 的项目；当前业务内容和 Git 来源尚待设备 B 回读。

## 已确认事实

- 左侧显示名称为 `aidrama`。
- 该项目当前存在于设备 B；设备 A 尚无对应本地项目。
- 它与本仓库管理的全局 `aidrama` Skill 不是同一对象，不能用 `全局技能/aidrama` 冒充项目根目录。
- 用户已把公共项目定义为 A、B 可同步项目的并集，因此该项目进入项目清单版本 `4`。

## 尚未确认

- 设备 B 的真实项目根目录和本地 `projectId`。
- 项目包含的 Git 仓库、remote、默认分支和 clone 地址。
- 项目目标、当前工作内容、验证方式和未完成任务。
- 设备 A 应使用的最终本地目录名。

## 同步状态

- 设备 B：左侧项目已由用户确认存在，详细映射待回读。
- 设备 A：阻塞，必须等待真实 Git 来源；不得创建空壳或复制 Skill 目录。
- 共同长期任务：当前未登记。

## 精确下一步

1. 设备 B 下次“开始同步”时读取正式项目列表，记录真实路径和 `projectId`。
2. 在真实项目根目录发现全部 Git 仓库，记录每个 remote、默认分支和当前提交；没有 remote 时如实标记来源阻塞。
3. 把可用 clone 地址推送到项目并集清单和设备映射。
4. 设备 A 拉取记录后，把真实项目克隆到 `~/Desktop/DEV/Life/CodexSpace/`，通过正式入口加入 Codex 项目列表并回写本地 `projectId`。
