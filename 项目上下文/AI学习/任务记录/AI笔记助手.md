# 长期任务：AI 笔记助手

> 设备 A 个人冻结记录：双设备同步流程和设备 B 不读取、不更新、不创建。本文件只保留历史快照。

## 元数据

- 逻辑任务 ID：`ai-learning-notes`
- 设备 A 线程 ID：`019f68ff-3646-7902-b978-2c1fda900b38`
- 所属项目：AI学习
- 原工作目录：`/Users/qichao/Documents/AI学习`
- 状态：进行中

## 目标

作为教学任务的独立笔记执行器，只在用户明确授权“记笔记”时，实际维护 BoardMix「AI笔记」思维导图。

## 固定要求

- 不自行记录，必须有明确授权。
- 不能只输出 Markdown，必须实际操作导图节点。
- 顶层分类包括：AI 应用开发、后端工程、模型训练。
- 复用、合并和重命名已有节点，避免重复。
- 只操作笔记，不修改项目业务代码。
- 登录、权限、画布定位或编辑失败时如实报告。

## 已确认结论

- BoardMix 使用 WebGL 画布，缺少普通 DOM；自动化主要依赖坐标、截图和键盘焦点。
- 控制缓慢主要来自画布控制困难和通道超时，不只是网速。
- 不同 Codex 任务的浏览器标签页隔离，笔记任务需要自己的 BoardMix 标签页。

## 已完成

“后端工程”分支已经成功整理：

- Python 基础
- HTTP 与 FastAPI
- 工程化基础
- 数据库
- 测试
- Docker 与服务器部署

第二次任务已部分完成：

- 在“后端工程 → Python 基础”下建立“类型、对象与接口调用链”主节点。
- 删除错误重复分支。

## 尚未完成

“类型、对象与接口调用链”仍需补齐：

1. 类型、类、实例、变量
2. JSON 与 Python 对象转换
3. 路由函数两个参数的来源
4. 正向调用链
5. 反向返回链
6. Service 接口与具体实现

可能仍有一个默认“子主题”占位未核验。

## 精确下一步

1. 恢复笔记任务的浏览器控制。
2. 清理可能残留的“子主题”。
3. 核对主节点层级。
4. 完成六组详细子节点。
5. 最终截图核验、去重并确认保存。

## 设备 B 导入提示

```text
读取 AI笔记助手.md，把它视为已经确认的笔记进度。不要自动操作 BoardMix，只回复“笔记上下文已导入，等待明确授权”。
```

## 安全检查

- 未保存 BoardMix 私有页面 ID。
- 未保存密码、令牌或私人联系方式。
