# 项目：AI学习

最近更新：2026-07-28

## 一句话说明

以前端开发者为起点，通过“英语 AI 陪练”实战项目逐步学习 Python、FastAPI、大模型应用、Agent、部署和后续模型训练。

## 本地位置

```text
~/Documents/AI学习
```

主要代码目录：

```text
~/Documents/AI学习/english-ai-coach
```

## 当前同步阻塞

只读检查没有在 `AI学习` 或 `english-ai-coach` 中发现 `.git` 仓库，也没有可核验的 Git remote。

这与“所有项目都由 Git 同步”的预期不一致。设备 B 接入前必须：

1. 确认是否存在尚未迁入当前位置的 Git 仓库。
2. 若不存在，为 `english-ai-coach` 建立私有 Git 仓库。
3. 提交前确认 `.env`、API Key、缓存和虚拟环境均已忽略。
4. 将远程地址和目标路径补入共同项目清单。

在这个问题修复前，不在设备 B 创建空的 `AI学习` 项目冒充同步完成。

## 长期任务

- [AI 教学助手](任务记录/AI教学助手.md)
- [AI 笔记助手](任务记录/AI笔记助手.md)

## 当前学习路线

1. Python 与 FastAPI 后端。
2. 大模型调用、Prompt 和结构化输出。
3. 数据库、测试、Docker、Linux 与服务器部署。
4. RAG、Tool Calling、Agent 和语音。
5. PyTorch、Transformer、SFT、LoRA 等模型训练。

## 当前工程进展

- 英语陪练 FastAPI 后端 MVP 已跑通。
- Mock 和豆包真实模型调用均已验证。
- 当前测试基线为 `7 passed`。
- API Key 放在被忽略的 `.env`，不得进入同步仓库。
- 前端聊天、数据库、登录、语音、RAG、Agent、部署和模型训练仍待后续。

