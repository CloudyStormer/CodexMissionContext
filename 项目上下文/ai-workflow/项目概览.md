# 项目：AI工作流（试点）

最近更新：2026-08-01

## 一句话说明

设备 A 上用于研究、走通和评测 AI 软件工程工作流的实验室；`projects/` 是验证样本，`control-center/` 是治理可视化工具，不以批量生产业务项目为首要目标。

## 映射与代码来源

- 设备 A 根目录：`/Users/qichao/Desktop/DEV/Lenovo/AIWorkFlow`，`projectId=d43e856c-a6e4-4ab5-8857-041fd346b853`。
- 超级无敌帅超超总于 2026-08-01 明确：最初设计的 Git 边界是 `AIWorkFlow` 根目录整体仓库，根 `.git` 指向一个项目；当前状态不得再解释为已经确认的多仓目标架构。
- 当前文件系统中的根 `.git` 已不存在；现状只能确认 3 个独立子仓库。`projects/ai-english-learning/.git` 创建于 2026-07-31 11:58，其初始提交树从一开始就只覆盖该子目录。现有证据不能证明根 `.git` 在何时、为何或由谁消失：
  - `projects/ai-english-learning`
    - 标准来源：`git@github.com:CloudyStormer/AI-Workflow-Dev.git`
    - 默认分支：`main`
    - 设备 A 与远端均为 `2182e79`；固定角色池和下游回退门的完整规则位于提交 `32ebe4e`，`2182e79` 仅合并已发布的早期治理提交历史；行内字母槽产品变更提交为 `a1d0444`。
    - 随机字母提示已在 `3c181ef` 完成并推送；行内字母槽填空已按用户指令回退产品阶段，`docs/01-prd.md` v1.1 等待超级无敌帅超超总审核。原 `docs/03-ui-prompt.md` 已暂停，产品获批前不得继续 UI/UX 或前端实现。
  - `projects/ai-model-radar`
    - 本地 `main` 为 `a32da87`；市场调研已完成并等待超级无敌帅超超总审核。
    - 当前无 Git 远端，无法同步到设备 B。
  - `control-center`
    - 本地 `main` 为 `da600ca`；当前无普通 Git 远端；本轮只更新治理规则，未重发或部署站点。
    - 私有演示站点版本 3 已上线：`https://ai-workflow-control-center.honest-flute-5906.chatgpt.site/`；数据明确标记为演示数据。
    - UI/UX 提示词已提交，仍等待超级无敌帅超超总审核，未擅自继续重设计。
- 英语项目 Git 远端信息：
  - 标准来源：`git@github.com:CloudyStormer/AI-Workflow-Dev.git`
  - 默认分支：`main`
  - 远端当前提交：`2182e79`
  - 设备 A 当前提交：`2182e79`
- 设备 B 目标根目录：`D:\.aaProject-Bruce\AIWorkFlow`。

## 同步状态

三个项目现已采用同一治理外壳：`project.yaml`、项目级 Skill、共享子 Skill、`workflow/` 五文件、scripts/tests/output；实现目录按 `split-web` 与 `sites-fullstack` Profile 保留真实差异。根级 `workflow-project-init` Skill 已完成新项目、幂等、冲突身份和缺失模块前向测试，并安装到设备 A 全局 Skills。

全局角色 Agent 池已固定：侧边栏只使用 `00 包工头` 与 `01` 至 `11` 的 12 个既有角色任务，每个角色负责 AIWorkFlow 下所有项目。项目隔离只依靠项目级 Skill、`project.yaml`、`docs/` 与 `workflow/`，禁止按项目、需求、返工或阶段回退新增角色 Agent/任务。“独立角色任务”是指复用现有编号角色任务，例如所有产品工作统一交给 `03 产品经理`。

下游变更回退规则已写入根规范、13 个 Skill、三个项目快照、初始化模板和验证器：产品逻辑或 UI/UX 变更先冻结下游，依次由固定 `03 产品经理` 交付并审核、固定 `04 UI/UX设计师` 交付并审核，再由对应固定开发角色重新获批；不得由开发代改、自动跑完整链路或继承审批。

用户称呼规则已更新：从 2026-07-31 起，所有未来对话必须使用完整称呼“超级无敌帅超超总”，不得再使用旧称呼或自行简称。根规范、三个项目、全局 Skills、控制中心线上版本和 11 个其他角色任务均已收到更新；历史 approvals/events 保留原始事实。

设备 B 仍不能完整补齐：当前可用 Git 来源只覆盖英语样本。根整体仓库来源尚未恢复，来源补全前不得用只有一个子仓库的目录冒充完整项目，也不得把当前三个子仓库误定为用户认可的最终结构。

## 补齐设备 B 所需来源

设备 A 需要先确认并恢复用户原定的根整体仓库边界，设备 B 才能在 `D:\.aaProject-Bruce\AIWorkFlow` 还原完整项目。恢复前必须保留并核对以下现有历史：

1. 根目录中 `README.md`、`roles/`、`skill/`、`architecture/`、初始化模板和漂移校验等当前未被根 Git 管理的内容。
2. AI English Learning 现有独立历史及远端，不能因恢复根仓而覆盖或丢失。
3. AI Model Radar 当前本地 `main` / `a32da87` 与 Control Center 当前本地 `main` / `da600ca` 的独立历史；现有 Sites 私有源码与托管地址不能替代 Git 来源。

此前基于“根 `.git` 缺失”提出的多仓/子模块推荐属于临时推断，现已撤销，不再作为既定计划。任何补推前都应先确定根整体仓库的真实远端和恢复方式，再无损协调三个嵌套仓库的历史；设备 B 无法凭本机事实补造。

## 任务边界

设备 A 在该项目下有且只使用 12 个固定角色任务，并已按首次主要入场顺序编号：`00 包工头`、`01 市场调研员`、`02 项目经理`、`03 产品经理`、`04 UI/UX设计师`、`05 架构师`、`06 前端工程师`、`07 后端工程师`、`08 数据工程师`、`09 代码审查员`、`10 测试工程师`、`11 DevOps工程师`。12 个任务均已取消置顶并跨项目复用。误建的 `AI English Learning｜产品变更：行内字母槽填空` 任务 `019fb892-8981-74f0-a396-11a21b018c43` 已停止使用；该项目的产品职责和待审核状态已只读交回固定任务 `03 产品经理`（`019fb74a-9dbd-7e13-823f-80584d8ac1b7`）。历史任务保留，不删除、归档或改名。用户尚未要求固定角色任务成为双设备共同长期任务，因此不加入共同任务清单。

## 精确下一步

1. 超级无敌帅超超总审核 `projects/ai-english-learning/docs/01-prd.md` v1.1；产品变更通过后才可进入 UI/UX，UI 产物再次通过后前端才可编码。
2. 超级无敌帅超超总审核 `projects/ai-model-radar/docs/00-market-research.md`，通过后才允许项目经理入场。
3. 超级无敌帅超超总审核 `control-center/ui/01-control-center-ui-design-prompt.md`，通过后才允许 UI/UX 继续出设计。
4. 暂停把当前结构按多仓继续补推；先定位或恢复 `AIWorkFlow` 根整体仓库，再无损处理三个嵌套仓库和设备 B 来源。
5. 不删除、移动或改名设备 A 当前项目；不自动把 12 个角色任务升级为共同长期任务。
