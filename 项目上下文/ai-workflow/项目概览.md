# 项目：AI工作流（试点）

最近更新：2026-08-25

## 一句话说明

设备 A 上用于研究、走通和评测 AI 软件工程工作流的实验室；`projects/` 是验证样本，`control-center/` 是治理可视化工具，不以批量生产业务项目为首要目标。

## 映射与代码来源

- 设备 A 根目录：`/Users/qichao/Desktop/DEV/Lenovo/AIWorkFlow`，`projectId=d43e856c-a6e4-4ab5-8857-041fd346b853`。
- 唯一标准 Git 来源：`git@github.com:CloudyStormer/AI-Workflow-Dev.git`。超级无敌帅超超总于 2026-08-01 再次明确：Git 边界必须从 `AIWorkFlow` 根目录开始，根目录下全部框架、`projects/` 中全部子项目和 `control-center/` 只能共同推到这一个仓库；禁止给任何子项目建立独立推送目的地。
- 根仓已于 2026-08-01 无损恢复并整体推送：全目录只有根部一个 `.git`，从根目录和三个项目目录执行 `git rev-parse --show-toplevel` 均返回 `AIWorkFlow`；只有一个 `origin`。本机因 GitHub 多账号密钥使用 SSH Host 别名 `git@github.com-cloudystormer:CloudyStormer/AI-Workflow-Dev.git`，实际仓库仍是唯一标准来源。
- 设备 A 完成根仓恢复时、GitHub `main` 与全新验证克隆均为 `fa2294c127176255be29169edaf49f5cc6b020f5`，tree 为 `4fd94da072e8f503f39a52c97ad9b476cae2af7a`，跟踪文件 272 个；推送为从旧远端 `2182e79` 到新提交的普通 fast-forward，没有 force。设备 B 接入后修复 Windows Git Bash 的根路径等价判断并普通推送 `25d7ff563861e5bdceeeed040adb7ffa6973b41b`；设备 A 已在运行 `A-20260802T200017+0800` 快进到同一提交并重新验证。A、B 最近已发布状态与远端均为该提交，tree 为 `d0a52fc8d9f78a197e0623ba16920278226f50de`，跟踪文件仍为 272 个。
- 2026-08-03，设备 A 已把 English PRD v1.1 与 Radar 市场调研的真实审核通过记录提交并普通推送为 `7c20abee8a4f4ac326b4afd0c88b9dd81ee1d5de`，tree 为 `370284711febeb74fac44581abc880c5ad55e224`，跟踪文件仍为 272 个；A 与远端一致。设备 B 最近已知仍为 `25d7ff5`，须在下一次同步取得 `7c20abe`，不能提前声称双端一致。
- 同日，超级无敌帅超超总回复“前两个继续”，分别批准 English 的固定 `04 UI/UX设计师` 重新入场修订提示词、Radar 的固定 `02 项目经理` 入场制定项目计划；入场状态提交并推送为 `dd6d53e4612dc6e502b15e7ab8bc1281dd6c1ed6`，tree 为 `37ae7e4fc824983c80f65f6cde2c3d9bb3d1f326`，跟踪文件仍为 272 个。两个固定角色任务已分别收到精确交接并开始工作；此次继续不授权后续阶段。
- Radar 固定 `02 项目经理` 已完成 `docs/01-project-plan.md` v1.0，自查并以 `d2ecc081e8a4f67e82441be9fcefeaf87b456ade` 普通推送；计划 SHA256 为 `9320c2908843c69a5e754f66d786d7093a8ebd6e35b961c6d09366e290313166`，当前等待超级无敌帅超超总审核。计划明确当前无业务代码或运行入口；按最新流程规则，“通过”将同时授权唯一下一站固定 `03 产品经理` 编制 PRD，但不会跨过该站继续后续阶段。
- English 固定 `04 UI/UX设计师` 已完成 `docs/03-ui-prompt.md` v1.1，自查并以 `42569bd1` 普通推送；当前等待超级无敌帅超超总审核。提示词“通过”将立即授权同一固定 UI/UX 角色生成设计，设计交付后重新停门，前端继续冻结。
- 2026-08-03，超级无敌帅超超总明确“以后我说通过，你直接继续，不要等我说继续”。根规范、13 个 canonical Skill、三个项目快照、项目初始化模板、验证器和结构化状态契约已统一为“一跳续行”：明确“通过”同时批准当前产物并授权唯一、输入完整、非高风险的下一站；一次最多推进一个交付单元，下一站交付后重新审核，终点只结束本轮。生产发布、不可逆操作、付费、账号/凭证、隐私数据和对外发送仍须针对具体动作单独授权。该规则经正向、缺失策略、AGENTS 正文缺失、YAML 注释伪造反例和双重语义审计后，以 `30ff229a5b4b2d3c64b4c8359297fb1fda3cc709` 普通推送；tree 为 `4d38237f1580479d94bbfce81f081c4b8847d447`，跟踪文件 273 个，设备 A 与远端一致，设备 B 最近已知仍为 `25d7ff5`。
- 2026-08-04，固定 `05 架构师`按用户指定顺序完成两个独立架构单元并分别停在 `architecture-review`：Frontend Career Radar `projects/market-analysis-dev/docs/04-architecture.md` v1.0，SHA256 `bcc782409ebde28e003c9e4a1c20d45ddcb3b787f8a2de020c451ef05957d144`，提交 `93fedbb`；AI Model Radar `projects/ai-model-radar/docs/04-architecture.md` v1.0，SHA256 `155e734351c753cd9a51878f9b1549f0d7aaf4db18872c35f8763ff39903db04`，提交 `7b2c756`。两次均通过项目结构、Git 边界、Skill 漂移、YAML/JSONL、Markdown 与 scoped diff 验证并普通推送，远端与本地提交一致。Career 固定“职业方向 → 技术栈”，以静态证据内容和浏览器内隐私工作台为核心；Radar 固定为静态人工快照 Web，真实采集、后端、数据库与部署后置。用户本轮明确要求即使通过也不得自动路由项目拆解或开发。
- 2026-08-04 后续，English 记忆曲线闭环 PRD v1.3 已以 `cb705c4` 推送并停在 `product-change-review`；Control Center 六个简体中文治理视图已通过 lint、build、3/3 测试和浏览器巡检，以 `a35316d` 推送并停在 `frontend-delivery-review`。English reveal-answer 的 3 个跟踪修改和 3 个未跟踪文件因产品输入变化被明确冻结，状态为 `frozen-uncommitted-worktree-preserved`，不得在同步任务中测试、提交或推送。
- 2026-08-06，AIWorkFlow 局部 `ai-dev-workflow` Skill 新增根项目“汇报”口令：未点名子项目时动态扫描 `control-center/project.yaml` 与 `projects/*/project.yaml`，逐项目汇报阶段、角色、产物、审核门、Web／服务／部署和风险，最后给出总体协调；汇报本身只读，不自动推进。规范源、四项目快照、全局副本与 Skill Lock 已同步并验证，提交 `0110205` 已推送；A 与远端一致，B 最近已知仍为 `a35316d`。
- 四个项目现均为根仓普通目录：
  - `projects/ai-english-learning`
    - 原远端主线 `2182e79` 已作为新根仓祖先保留，原顶层内容已整体移动到该路径；依赖锁文件已纳入根仓。
    - 随机字母提示和行内字母槽系列是历史产物。当前 `docs/01-prd.md` v1.3 已以 `cb705c4` 推送，引入 D+1/D+3/D+7/D+14、D+30 与 36 条验收标准，停在 `product-change-review`；reveal-answer 的 6 项前端现场继续冻结。
  - `projects/market-analysis-dev`
    - Frontend Career Radar 已完成调研、计划、PRD、UI 与架构 v1.0；架构提交 `93fedbb` 已推送并停在 `architecture-review`。核心顺序固定为“职业方向 → 技术栈”，未启动开发或部署。
  - `projects/ai-model-radar`
    - 原本地主线 `a32da87` 已通过 subtree 历史导入成为新根仓祖先；市场调研、项目计划、PRD、UI Prompt 与 9 张视觉基线均已完成相应审核。固定 `05 架构师`已交付 `docs/04-architecture.md` v1.0 并停在 `architecture-review`；真实来源、后端、数据库、影子运行与部署均未开始。
  - `control-center`
    - 原本地主线 `da600ca` 已通过 subtree 历史导入成为新根仓祖先。
    - 私有演示站点版本 3 已上线：`https://ai-workflow-control-center.honest-flute-5906.chatgpt.site/`；这是 Sites 网页部署，不是 Git 推送，数据明确标记为演示数据。
    - 六个完整简体中文治理视图已通过 lint、build、3/3 测试和浏览器巡检，以 `a35316d` 推送并停在 `frontend-delivery-review`，未自动进入代码审查或部署。
- 设备 B 根目录：`D:\.aaProject-Bruce\AIWorkFlow`；完整根仓已实际克隆。

## 同步状态

2026-08-25 设备 A 已安全取得远端并快进至 `main` / `45943ca`；工作树干净，ahead/behind `0/0`。本轮没有代替各业务任务继续遗留工作。

四个项目现已采用同一治理外壳：`project.yaml`、项目级 Skill、共享子 Skill、`workflow/` 五文件、scripts/tests/output；实现目录按各自 Profile 保留真实差异。根级 `workflow-project-init` Skill 已完成新项目、幂等、冲突身份和缺失模块前向测试，并安装到设备 A 全局 Skills。

全局角色 Agent 池已固定：侧边栏只使用 `00 包工头` 与 `01` 至 `11` 的 12 个既有角色任务，每个角色负责 AIWorkFlow 下所有项目。项目隔离只依靠项目级 Skill、`project.yaml`、`docs/` 与 `workflow/`，禁止按项目、需求、返工或阶段回退新增角色 Agent/任务。“独立角色任务”是指复用现有编号角色任务，例如所有产品工作统一交给 `03 产品经理`。

下游变更回退规则已写入根规范、13 个 Skill、四个项目快照、初始化模板和验证器：产品逻辑或 UI/UX 变更先冻结下游，依次由固定 `03 产品经理`、固定 `04 UI/UX设计师` 和对应固定开发角色处理。每站明确“通过”同时批准当前交付并自动授权紧邻的唯一下一站，无需再说“继续”；一次只推进一站，下一站交付后重新停门，不得由开发代改或自动跑完整链路。

用户称呼规则已更新：从 2026-07-31 起，所有未来对话必须使用完整称呼“超级无敌帅超超总”，不得再使用旧称呼或自行简称。根规范、三个项目、全局 Skills、控制中心线上版本和 11 个其他角色任务均已收到更新；历史 approvals/events 保留原始事实。

设备 B 已从唯一标准地址完整克隆到 `D:\.aaProject-Bruce\AIWorkFlow`，并正式登记为 `projectId=e7d65417-ea9a-47b3-8e2f-87da309b8406`；12 个固定角色任务和历史停用任务均已在正确 D 盘项目建立。A 与远端当前为 `0110205`，B 最近已知为 `a35316d`。B 的实体映射已齐，但运行 `A-20260804T180316+0800` 未精确回读 13 个任务正文，语义为 `UNAVAILABLE`；完整语义、任务顺序和项目布局须在新运行中重新快照和回执。

## 唯一总仓库恢复结果

设备 A 只使用 `git@github.com:CloudyStormer/AI-Workflow-Dev.git` 这一个根整体仓库；不得再为 Radar、Control Center 或其他未来子项目配置独立远端。本次恢复保全了：

1. 根目录 `README.md`、`AGENTS.md`、`roles/`、`skill/`、`architecture/`、项目模板、边界检查和漂移校验。
2. AI English Learning 原远端历史 `2182e79`，以及 Radar `a32da87`、Control Center `da600ca` 两套原本地主线历史。
3. 三份旧 `.git` 的完整压缩归档、bundle 与原始元数据目录，保存在根仓本地 `.git/migration-backups/`，不进入 GitHub。

根级 `scripts/check-git-boundary.sh`、`AGENTS.md`、项目模板和 `workflow-project-init` Skill 已锁定单根仓规则；所有项目快照、锁文件和设备 A 全局 Skill 已同步。此前的多仓/子模块推荐已撤销。

## 任务边界

设备 A 在该项目下有且只使用 12 个固定角色任务，并已按首次主要入场顺序编号：`00 包工头`、`01 市场调研员`、`02 项目经理`、`03 产品经理`、`04 UI/UX设计师`、`05 架构师`、`06 前端工程师`、`07 后端工程师`、`08 数据工程师`、`09 代码审查员`、`10 测试工程师`、`11 DevOps工程师`。12 个任务均已取消置顶并跨项目复用。误建的 `AI English Learning｜产品变更：行内字母槽填空` 任务 `019fb892-8981-74f0-a396-11a21b018c43` 已停止使用；该项目的产品职责和待审核状态已只读交回固定任务 `03 产品经理`（`019fb74a-9dbd-7e13-823f-80584d8ac1b7`）。历史任务保留，不删除、归档或改名。按用户 2026-08-01 的最终定义，12 个固定角色和该历史停用任务都必须在设备 B 的 AIWorkflow 项目中可见，并分别写入可直接续接的精简交接；历史停用任务只做实体镜像，不成为第 13 个角色，也不自动继续旧业务。

## 精确下一步

1. 超级无敌帅超超总审核 English `docs/01-prd.md` v1.3，选择“通过 / 修改 / 打回”；通过只一跳授权固定 `04 UI/UX设计师` 修订复习反馈、日程和薄弱词提示词，固定 06 继续冻结。
2. 超级无敌帅超超总分别审核 Frontend Career Radar 与 AI Model Radar 的 `docs/04-architecture.md` v1.0；本轮特殊约束是通过后也不自动路由项目拆解或开发。
3. 超级无敌帅超超总审核 Control Center 提交 `a35316d` 的前端交付；通过后才进入固定 `09 代码审查员`，不自动部署。
4. 设备 B 直接使用既有正式 D 盘项目和 13 个任务镜像；最近已知代码为 `a35316d`，下次先安全快进到 `0110205` 或届时远端最新提交，再以新运行 ID 精确读取 13 个任务正文并生成本端快照；不得重复克隆、创建任务或复用 `UNAVAILABLE` 哨兵。
5. 不删除、移动或改名任何现有项目或任务；同步镜像不授权继续旧业务需求。
