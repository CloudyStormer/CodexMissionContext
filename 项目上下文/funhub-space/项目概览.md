# 项目：花果山社区

最近更新：2026-08-02

## 一句话说明

设备 B 中由八个独立 Git 仓库组成的社区及衍生应用项目集合。

## 映射

- 设备 B：`D:\.aaProject-Bruce\funhub-space`，`projectId=local-90e565842c3b3296df299d30317505ec`。
- 设备 A：`/Users/qichao/Desktop/DEV/Life/CodexSpace/funhub-space`，`projectId=996beee0-c57a-420e-bbad-861a74659fef`。

## 设备 A 已取得仓库

| 仓库 | 分支 | HEAD |
|---|---|---|
| `aime-bridge-backend` | `main` | `703f0c16d7bc` |
| `funhub` | `Feature/funhub-taro-migration-1.0` | `5111563` |
| `funhub-CandyArt` | `main` | `53eba8fd7042` |
| `funhub-taro` | `main` | `f858422e1846` |
| `funhub-WordSmiths` | `main` | `c9802350a87c` |
| `funhub-WordSmiths-backend` | `main` | `4138c7322fbe` |

六个仓库均与各自 upstream 一致且工作树干净。设备 A 已用包含这些真实仓库的项目根目录完成正式登记。`funhub` 已取得设备 B 推送的提交，但当前 HEAD 的差异检查仍有两处行尾空白。

## 当前阻塞

- 设备 A 以确认可用的 `CloudyStormer` SSH 身份重试后，`english-talk-trainer` 与 `MountainFruitCottage` 对应远端均返回 `Repository not found`；两个目录没有创建。
- `aime-bridge-backend/.env` 正在被 Git 跟踪；当前敏感字段为空，但历史版本曾出现非空敏感值，须轮换历史凭据、停止跟踪并改用安全模板。
- `funhub-WordSmiths-backend/.env` 当前含真实敏感值；`app/data/topic_chat_sessions.json` 也在跟踪运行时聊天/会话数据。两者均为安全或隐私硬阻塞，不得由同步任务盲目提交。
- `funhub-taro` 的三个 `.env.*` 当前仅含注释模板，但仍应改为 `.example` 并忽略真实环境文件，防止后续误提交。
- 其余仓库已经取得远端状态；另一设备未推送的本地内容继续保持不可见边界。
