# 任务：第 014 集反向 Buff 与六视图身份锁

## 元数据

- 任务编号：`20260806-A-episode-014-reverse-buff-multiview-lock`
- 项目：`vid-mat-lab`
- 设备：A
- 更新时间：2026-08-06 +08:00
- 状态：第 014 集完整生产包、10 张关键帧与永久六视图已完成并推送；动态、实际声音锚点和成片待生成
- 项目提交：`9e590f2`，已推送 `origin/main`

## 用户要求与永久决定

1. 新思路为“在外面没人惯着你”触发反向 Buff：NPC 嘴上不惯着，实际不断给小耗儿发福利。
2. 小耗儿形象必须完全符合永久原版；不能把长手、换脸、纹路跳位或动态过程中变得不像主角的素材交付为可用。
3. 建立正、背、左、右、左 3/4、右 3/4 六视图。以后每张新关键帧同时引用永久原版与对应角度图，多视图只能辅助角度，不能成为新身份源。
4. 每条视频提示词必须独立包含逐帧身份锁；源片和最终视频必须有非空音轨，同一角色跨 clip 使用同一已验收实际声音资产。

## 已完成

1. 保留用户原稿，完成时长审计并锁定 21.0 秒生产版：0.6 秒后期标题卡＋10 个 AI 片段，口播共 75 个汉字。
2. 完成制作简报、剧本、口播、SRT、连续性、10 行分镜、10 条图片提示词、10 条独立图生有声视频提示词、剪辑、声音、锚点登记、发布和交付说明。
3. 生成并验收 10 张 941×1672 正式关键帧。04、09、10 的长臂初版被判退并归档；正式版改为短手贴身且道具独立承重，04 的 NPC 另改为闭嘴轻皱眉。
4. 建立 `assets/brand/resistor-mascot-multiview-v1/`：正面 1080×1080 与永久原版 SHA-256 完全一致；其余五张 1254×1254、暖白净底、无文字或水印。
5. 更新项目 `AGENTS.md`、视频流程与品牌锁定文档；新增 `MULTIVIEW_IDENTITY_LOCK.md`，记录六视图方向、像素、SHA-256、引用优先级和动态五点抽帧规则。
6. 更新全局 `xiaoneihao-video-creator` Skill，新增多视图选择和完整图片/视频身份锁模板；大角度转身必须拆镜并硬切。

## 验证

- `aidrama` 分镜校验：10 行、AI 片段合计 20.4 秒、加 0.6 秒后期卡后为 21.0 秒、0 个警告。
- SRT 10 段，结束时码 21.000 秒；口播共 75 个汉字。
- 视频提示词 10 条、逐帧身份锁 10 块、角度参考 10 块、有声硬要求 10 块、实际声音资产字段 10 处、自动 BGM 禁令 10 处。
- 图片提示词 10 条、多视图身份锁 10 块；正式关键帧 10 张均为可解码 941×1672 PNG。
- 六视图文件 6 个；正面副本和永久原版 SHA-256 均为 `c5331da5f48b986afe738123e5c876b0ea7f6bbd5cf0336009732cd2ccd1dc0b`。
- 本集和品牌 Markdown 相对链接检查 83 项，缺失 0；`git diff --check` 通过。
- Skill 通过 `quick_validate.py`；无上下文前向测试正确拆分正面对白与背面行走，没有在单片中生成大角度转身。

## 当前阻塞与下一步

1. `VO-XH-01` 与 `VO-NPC-01` 仍只是内部配置编号，平台实际 `provider_voice_id` / `reference_audio`、模型、哈希和 seed 未登记，因此不得批量生成动态，也不能声称已锁定声纹。
2. 先为两套声音各制作并人工验收一条 6—10 秒无 BGM 干声锚点。
3. 先试生镜头 01 与 03，检查逐字对白、无牙口型、非空音轨、固定音色和五点抽帧身份。
4. 合格后生成其余 8 条源片，按 21 秒时间轴剪辑、混音、导出和发布。

## 安全边界

- 没有伪造平台 Voice ID 或声纹锁定结果；声音未登记状态已写入本集 README、锚点表和交付说明。
- 没有提交密码、令牌、Cookie、私钥、浏览器状态或缓存。
