# 任务：多年项目只读代码审查

## 元数据

- 任务编号：`20260807-A-lenovo-meta-code-review`
- 项目：`lenovo-meta-front-end`
- 设备：A
- Codex 线程 ID：`019fd684-8415-7833-9ab0-ee8b5157291e`
- 开始时间：2026-08-07
- 更新时间：2026-08-07
- 状态：已完成
- 代码仓库、分支与开始提交：`forThree159` / `8d6235d4118c8e7f30dafa5bd8adaa4b77653d8b`
- 结束提交：未产生项目代码提交

## 本次目标

只读审查多年演进的前端项目，识别安全、正确性、架构、重复、性能、依赖、测试与文档问题，说明可保留优势并给出渐进治理顺序；不得修改项目代码。

## 用户要求

- 从专业开发角度审核多年累积的代码垃圾、逻辑垃圾和冗余逻辑。
- 给出不足、优化空间和应保留优势。
- 输出报告，不修改项目代码。

## 开始前读取的上下文

- 项目此前未有业务审查记录；本轮开始时完成设备 A 上下文同步并保留项目原有 Git 现场。
- 项目工作树原有 `webpack.config.js` 未提交修改，分支较 upstream 领先 1 个提交。

## 已完成内容

1. 盘点架构、路由、API/Redux、构建、依赖、资源和 Git 规模。
2. 并行审查重复/疑似孤儿文件、安全、XSS、资源释放、错误处理、测试和 CI。
3. 交叉验证关键缺陷并形成 P0/P1/P2 分级与分阶段治理建议。
4. 未修改项目文件，未运行会清理并重写约 14 GiB `dist/resource` 的生产构建。

## 关键决定与原因

| 决定 | 原因 | 影响 |
|---|---|---|
| 不建议全量重写 | 项目沉淀大量真实 3D/RTC/AR/AI 业务资产，且缺少回归测试 | 采用先止血、补验证、再绞杀式拆分 |
| 凭证与 XSS 定为 P0 | 浏览器包可读取多组凭证，且存在远程/用户内容直接注入 HTML | 修复前不应视为安全上线基线 |
| 不按文件名或零入向结果直接删除 | 存在动态脚本、字符串路由和外部 HTML 加载；`OperationMenuOld` 实际仍在主路径使用 | 所有减重先做可达性与烟测验证 |
| 资源治理优先于格式清理 | 跟踪内容约 15 GiB，重复 Blob 约 1.48 GiB，构建全量复制资源 | 优先对象存储/CDN、manifest 和新增大文件门禁 |

## 文件与外部动作

| 文件或对象 | 动作 | 作用 |
|---|---|---|
| 项目仓库 | 只读扫描 | 未修改、未暂存、未提交、未推送 |
| npm 官方 registry | 只读 audit | 生产依赖口径 68 项告警；全依赖口径 115 项告警 |
| 本任务记录与项目概览 | 更新 | 保存可续接的压缩审查事实，不记录任何凭证值 |

## 命令、测试与证据

| 检查 | 结果 | 证据或说明 |
|---|---|---|
| Git 状态 | 项目仍为 `forThree159` ahead 1，只有原有 `webpack.config.js` 修改 | 审查未改变现场 |
| 规模 | 6,631 个跟踪文件；约 15 GiB 跟踪内容；`.git` 约 28 GiB | 大型二进制主要集中于 `app/resource` |
| 重复 | 925 组重复 Blob，约 1.48 GiB 逻辑冗余；源码有大量跨业务复制 | 删除前必须验证引用 |
| 测试 | 未发现真实 Jest 测试或 CI；测试脚本缺少 Jest 本体而失败 | 当前没有可用回归门禁 |
| 依赖 | `npm ls --all` 返回 `ELSPROBLEMS`；官方 audit 全依赖 115 项 | audit 是 advisory 匹配，仍需逐项验证可利用路径 |
| 安全 | 多组前端凭证与三条未清洗 HTML 注入链已确认 | 具体秘密值未写入本记录 |

## 失败、修正与经验

- 生产构建会先清理 `dist` 并全量复制约 14 GiB 资源，不适合作为本次只读审查验证；本轮明确未执行。
- 静态零入向扫描可能遗漏运行时字符串加载，结果只能作为候选删除清单，不能作为直接删除授权。
- npm 默认镜像不支持 audit 接口，改用官方 registry 完成只读版本告警检查。

## 尚未解决

- P0：服务端吊销/轮换前端暴露的 JWT、AI SDK key/secret 和管理密码，并迁出浏览器端。
- P0：取消或白名单清洗 AI Markdown 与两套评论区的 HTML 注入。
- P1：修复事件/定时器/WebGL/视频资源释放、loading 计数、清理作用域和恒假环境判断。
- P1：建立关键 URL 烟测、资源 404 检查、CI、lint、secret scan 和依赖升级矩阵。
- P1/P2：资产迁对象存储/CDN，路由/API/Redux 按领域拆分，逐组收敛复制实现。

## 精确下一步

1. 只有用户明确通过审查结论并授权修复后，才进入代码变更。
2. 第一修复批只处理凭证轮换协作、XSS 与确定性生命周期缺陷；每项建立烟测，禁止夹带大重构。
3. 第二批建立质量门禁和资产 manifest，再对疑似孤儿与重复文件逐项验证。

## 给另一台设备 Codex 的继续指令

```text
先读取本记录与项目概览，确认项目 Git 仍保留用户现场。若用户未授权修复，只引用审查结论，不改代码。获得授权后先处理 P0 凭证轮换与 XSS，再建立关键 URL 烟测；禁止直接删除零入向、copy、old 或重复哈希文件。
```

## 安全检查

- [x] 未记录密码、令牌、Cookie、私钥或账号缓存。
- [x] 未复制原始聊天全文。
- [x] 项目 Git 状态和上下文 Git 状态已经如实记录。
