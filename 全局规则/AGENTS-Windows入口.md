# Windows 全局 Codex 同步入口

本文件是由 `CodexMissionContext` 安装器管理的稳定入口，不是权威规则正文。

处理任一 Codex 任务前，必须完整读取并遵守：

```text
D:\.aaProject-Bruce\CodexMissionContext\全局规则\AGENTS.md
```

设备 A、B 完全平等，用户会交替使用；任一设备都要从远端最新上下文继续并及时更新。

如果上述 D 盘仓库或规则文件不可访问，必须准确报告阻塞，不得改从 C 盘创建共同项目，也不得凭记忆猜测同步规则。
