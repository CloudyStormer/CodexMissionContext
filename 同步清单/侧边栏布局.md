# 双设备共享侧边栏布局

布局版本：3
前序布局版本：2
布局状态：有效
规范化布局 SHA-256：`5310e24b57cfc72e401f56e2e6d775351fa701fd8fb0d1f451b62ca92cb5d81e`
来源候选：`侧边栏布局候选/2-A-20260802T134623Z-full39.md`
发布设备：A
发布日期：2026-08-02
基准项目清单版本：9
基准任务清单版本：5
完全对齐交接包版本：协议 `1`、目标修订 `1`

本文件只保存共享对象的逻辑 ID、顺序和置顶状态，不保存或复制 Codex 原始全局状态、本地数据库、session 或缓存。

> 2026-08-11 目标已升为交接包协议 `1` / 目标修订 `3`，项目清单 `12`、任务清单 `10`，共 14 项目 / 44 任务。本文件仍是基于旧修订 `1` 的有效布局版本 `3`，只覆盖 11 项目 / 39 任务；在新运行生成 A/B 完整快照、唯一合并包并按两阶段协议晋升新版本前，不得用版本 `3` 声称当前目标已全部对齐。

## 共享项目置顶顺序

从上到下：

1. `mission-context`（CodexMissionContext）
2. `ai-workflow`（AI工作流（试点））
3. `vid-mat-lab`（A 显示“创意帮”，B 显示“账号养成变现”）

除上述三项外，其他共同项目均为非置顶。设备 A 本地置顶的个人项目 `AI学习` 永久排除，不进入设备 B，也不影响共享顺序比较。

## 共享项目非置顶顺序

从上到下：

1. `aidrama-project`（AI 漫剧）
2. `liquidity-watermark-assistant`（变现 - 去水印）
3. `liquidity-portrait`（变现 - 证件照）
4. `liquidity-bmi`（变现 - BMI）
5. `canvas-garment`（画布成衣）
6. `funhub-space`（花果山社区）
7. `lottery`（彩票玄学 / lottery）
8. `demo`（演示 - 多垂直赛道）

`demo` 当前在设备 A 尚无可克隆 HEAD，因此保留在期望顺序末尾；来源就绪并加入后按该位置应用。

## 同步范围任务置顶顺序

无。当前 39 个同步范围任务全部应为非置顶。

## 同步范围任务非置顶顺序

从上到下：

1. `mission-context-current-sync`
2. `mission-context-maintenance`
3. `ai-workflow-00-foreman`
4. `ai-workflow-01-market-researcher`
5. `ai-workflow-02-project-manager`
6. `ai-workflow-03-product-manager`
7. `ai-workflow-04-ui-ux-designer`
8. `ai-workflow-05-architect`
9. `ai-workflow-06-frontend-engineer`
10. `ai-workflow-07-backend-engineer`
11. `ai-workflow-08-data-engineer`
12. `ai-workflow-09-code-reviewer`
13. `ai-workflow-10-test-engineer`
14. `ai-workflow-11-devops-engineer`
15. `ai-workflow-retired-inline-letter-slots`
16. `vid-mat-lab-foreman`
17. `vid-mat-lab-foreman-2`
18. `aidrama-negative-review-reaper`
19. `aidrama-consultant`
20. `aidrama-worker-comeback`
21. `aidrama-ninth-lesson`
22. `watermark-foreman`
23. `watermark-frontend`
24. `watermark-backend`
25. `portrait-program-manager`
26. `portrait-foreman`
27. `portrait-frontend`
28. `portrait-backend`
29. `bmi-foreman`
30. `bmi-frontend`
31. `bmi-backend`
32. `canvas-garment-foreman`
33. `funhub-space-foreman`
34. `funhub-repair-computer`
35. `funhub-press-to-talk-fix`
36. `funhub-aime-frontend`
37. `funhub-aime-backend`
38. `lottery-foreman`
39. `demo-foreman`

## 任务并集版本 5 扩展状态

用户已于 2026-08-01 把“开始同步”最终定义为：两端同步范围内全部侧边栏任务都要可见，任务内有可直接续接的精简语义交接，并且共享任务的置顶状态和相对顺序一致。因此任务清单版本 `5` 已不再区分长期任务与临时任务。

运行 `A-20260802T200017+0800` 已取得 A/B 两份 39 项完整快照和唯一合并任务包；39 项 `merge_state` 全部为 `READY`。版本 `3` 候选先由提交 `04f114c` 单独发布，再次 fetch 确认版本 `2` 未变化且不存在同基线竞争候选后，按两阶段协议晋升为当前有效布局。

- 先按任务并集补齐所有可创建的可见镜像并发送精简交接。
- 已知同步范围任务的置顶状态要逐项核对；当前设备 B 正式列表为置顶 `0`。
- 版本 `3` 已把交接包确定的 39 项任务共享子序列晋升为有效规范；发布成功不等于任一设备已经应用成功。
- 正式任务列表若明确保证非置顶任务按最近更新时间返回，可按版本 `3` 从下到上串行发送同摘要短检查点后回读验证；活动任务或并发更新使结果不确定时仍标记“部分应用”。该方法不能用于项目顺序。
- 无法正式应用或逐项验证任务共享子序列时，必须继续标记“部分应用”，不得声称两边侧边栏顺序一致。
- A、B 的顺序回执只能在各自实际应用版本 `3` 后生成；缺失实体、正式排序能力或回读失败都保持部分应用。

## 设备自行确认的应用状态

| 设备 | 状态确认来源与时间 | 已完整应用版本 | 已部分应用版本 | 本设备已确认待应用版本 | 已部分应用未完成逻辑 ID | 本机上次确认布局基线 |
|---|---|---:|---:|---:|---|---|
| A | A 正式项目/任务列表与只读布局回读，2026-08-05 | 0 | 3 | — | `demo`、`demo-foreman` 缺失；38 个现存任务非置顶但实际共享子序列再次偏离版本 3；共享项目三项置顶正确，非置顶子序列为去水印→证件照→BMI→画布成衣→花果山社区→彩票→AI 漫剧，与版本 3 不同 | 项目置顶分量已部分确认；任务实体与置顶分量已部分应用版本 3，项目/任务顺序待新双端运行验收 |
| B | B 正式项目/任务列表与本轮快照，2026-08-04 | 0 | 3 | — | 39 个实体和非置顶已确认；本轮 39 项正文为 `UNAVAILABLE`、共享索引均为 `UNKNOWN`，项目顺序和项目置顶仍未知 | 任务置顶分量已确认无置顶；项目布局分量仍为版本 0，正文和顺序未验收 |

## 发布与分发状态

- 设备 A 在旧运行曾验证 38 个现存任务的版本 `3` 子序列；截至 2026-08-05，正常业务活动已再次改变实际顺序，项目非置顶子序列也与版本 `3` 不同。不把最近更新时间顺序冒充用户明确布局。
- 设备 B 已取得版本 `3`，正式登记 AIWorkflow 并补齐 39 个实体；本轮快照虽覆盖 39 项，但正文均为 `UNAVAILABLE`、共享索引均为 `UNKNOWN`，不能据此验收或应用布局。
- 运行 `A-20260804T180316+0800` 已固定为 39 项 `BLOCKED_UNAVAILABLE` 的阻塞审计。当前运行 `B-20260805T171708+0800` 的 A 快照已经通过，但 B 快照因目标顺序、三份截断正文和 `vid-mat-lab` 过期语义未通过输入验收；B 在同一运行刷新前不生成合并包、不投递正文、不生成成功回执，也不把当前实际顺序冒充版本 `3` 的完成应用。
- 两端共同阻塞仍包括：A 缺 Demo；项目任意排序/置顶和稳定任务排序没有正式能力；B 的部分本地仓库仍有冲突或脏改动。

## 版本规则

- 当前设备落后于另一端发布版本时，先执行本机基线与未发布变化检查；确认无冲突后再应用远端布局，不发布本机旧布局。
- 只有 `布局状态：有效` 的规范版本可以自动应用；状态为“冲突待裁决”时停止。非零本机基线必须能由设备记录引用的候选快照和摘要重建；无法验证时不得自动覆盖。
- 发布下一版本有三个并列分支：一是当前设备已完整应用最新版本且用户明确调整了可见共享布局；二是发布设备缺少并集对象，但其余可见共享对象已按最新版本验证，缺失对象保持上一版本的置顶区域及其相对每个既有共享对象的前后关系，且没有同基线并发冲突；三是任务清单从旧的部分任务基线迁移到完整并集，且同一运行 ID 的 A/B 完整任务快照、唯一合并任务包和交接包目标摘要都已取得。
- 第二个分支不得借部分视图移动缺失对象；第三个分支必须覆盖完整任务并集、保留旧任务置顶区域和相对顺序，并按交接包确定性项目分组顺序插入新任务。两者任何关系不能唯一保留时都停止发布并报告歧义；发布端仍按真实应用结果记账。
- 目标设备实际拉取新版本但尚未执行时，由目标设备记为“本设备已确认待应用”；实际尝试后仍缺对象才记为“已部分应用”。发布端只记录“待目标设备获取”，不得替另一端预报收件或应用结果。
- 单纯应用远端版本时不发布由部分视图产生的新版本。
- 部分应用不提升“已完整应用版本”；全部共享对象验证成功后才提升完整版本并清空部分、待应用和未完成状态。
- 应用时保持所有非共享对象的置顶状态及其在各自区域内的相对顺序，只重排共享对象子序列；不能保证时不执行整体重排。
- 同一旧版本被两端并发发布为不同下一版本时停止自动选择并报告冲突。
- 应用远端版本前，若当前可见共享布局已经偏离本设备上次确认的非零基线，也视为未发布本地候选；按 `同步清单/侧边栏布局候选/README.md` 持久化，不能静默覆盖。
- 基线版本 `0` 的首次对齐默认应用远端有效布局；用户明确要求保留本机当前布局时，先保存候选并停止自动应用。
- 新版本必须先单独提交并推送候选，再次 fetch 确认规范仍在前序版本且同基线候选唯一，才在第二个提交中提升为有效规范版本。晋升后才发现迟到的同基线候选时，不回滚已经应用的设备，而把规范状态改为“冲突待裁决”并暂停其他设备继续应用。
- 应用排序和置顶只能使用正式 Codex API 或用户可见界面；严禁直接写 Codex 原始状态或数据库。
