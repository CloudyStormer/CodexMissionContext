interface:
  display_name: "检查项目"
  short_description: "按完全对齐交接包同步双端代码、任务、语义和侧边栏顺序"
  default_prompt: "使用 $check-projects 执行‘开始同步’：先读取完全对齐交接包，以同一运行 ID 让两端同步全部共同项目代码，补齐可见任务镜像，把同一 SHA-256 的精简语义送达两端，再逐项验证项目与任务顺序、置顶及双端回执；任一项未完成就明确报告没有全部同步。"
