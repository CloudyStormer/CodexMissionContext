---
name: produce-aimg
description: 按已验证的 AIMG 工业流程，把用户提供的故事、小说、剧本、标题、角色参考图或旧项目材料整理为可审批、可追溯、可量产的 AI 漫剧项目。用户明确说“做AIMG”“按AIMG做”“AIMG设计”，或要求沿用《第九节课》《差评死神》《打工人逆袭》同类流程进行故事逻辑审查、项目建档、统一画风、人物抽卡、多视角角色基准图、分集剧本、分镜、提示词、样片和量产时使用。必须先通过故事逻辑门和主角共享画风、正脸并排审批，失败候选不得晋级正式锚点，样片通过后才能批量生产。
---

# produce-aimg

把 AIMG 视为一条有审批门的生产状态机，而不是一次性生成一堆剧本和图片。调用 `aidrama` 处理故事、剧本、分镜和交付；需要真实出图时读取并调用 `imagegen`。

## 执行状态机

`G0` 通过后，故事结构轨与角色视觉轨可按用户明确要求并行；两条轨道必须在 `G5` 前汇合：

```text
原始材料
→ G0 故事逻辑通过
   ├→ G1 全季结构通过 ─────────┐
   └→ G2 共享画风通过 → G3 双主角正脸共同通过 → G4 全部角色高清九宫格逐人通过 ─┤
                                                                                     └→ G5 剧本与分镜就绪
→ G6 静态首尾帧通过
→ G7 无声样片通过
→ G8 声音字幕与正式样片通过
→ G9 批量生产
→ 交付
```

任一门未通过，继续整理不依赖该门的文字，但禁止执行对应下游生成。`G5` 同时依赖 `G1` 与 `G4`；不要把“已生成”写成“已通过”。

## 1. 锁定输入与事实

先读取项目现有文件、用户最新口径、标题、正文、参考图和历史审批记录。区分：

- 用户明确事实：不得擅改。
- 工作假设：写入项目简报并说明理由。
- 未决问题：只有真正阻塞且无法合理假设时才询问，每次最多一个。

用户新修正覆盖旧稿时，保留版本记录并更新所有受影响的世界规则、时间线、知情状态、剧本、分镜和提示词；禁止新旧口径并行冒充有效。

## 2. 通过故事逻辑门

读取 [故事逻辑门](references/故事逻辑门.md)。重生、循环、穿越、记忆不对称题材必须明确：

- 谁触发、什么条件触发、何时回到哪里。
- 谁被重置、谁保留记忆、实体与技能是否跨轮。
- 每个角色在每个阶段知道什么。
- 每个关键事件的原因、结果和新增信息。
- 计数是否差一轮，随机事件是否稳定，干预是否产生蝴蝶效应。

先用通俗语言把故事完整串给用户。用户修正后更新真源并重新审计；用户明确说“开始弄吧”即表示按最新口径继续，不重复询问已确认事实。

## 3. 建立标准项目

读取 [目录与状态规范](references/目录与状态规范.md)。新项目可运行：

```powershell
python scripts/初始化AIMG项目.py --项目名 "项目名称" --输出目录 "目标父目录"
```

标准目录使用 `01_项目总览` 至 `11_交付文件`。候选、失败、正式素材物理隔离。已有项目只补缺失结构，不重排或覆盖用户文件。

全局写透、局部做细：完整故事做到路线图与时间线；首个叙事单元做到分集节拍；首 3 集做到制作剧本与分镜；先正式制作 1 集样片。

## 4. 建共享画风和角色审批

读取 [角色与画风审批](references/角色与画风审批.md)。在分别设计人物前先建立全剧唯一画风圣经，锁定脸部体系、写实度、眼睛比例、皮肤/头发/布料材质、焦段、测试光、色彩和禁止项。

第一轮只生成双主角正脸校准板：同一张图、同一批次、同一背景、同一灯光、同一镜头、同一渲染前缀。两人必须作为一组审批；任一人不通过，整组返工。不得先晋级一人再单独补另一人。

双主角正脸共同通过后，为每个具名或重复出场角色制作一张高清 3×3 九宫格基准图。固定按行排列：正面、背面、角色自身左侧、角色自身右侧、角色自身左前 3/4、右前 3/4、左后 3/4、右后 3/4、正面全身。左右一律以角色自身解剖方向命名，禁止镜像、翻转或只交换标签冒充另一侧。主角九宫格完成后再设计配角，并执行禁止同脸审查。完整协议见 [角色与画风审批](references/角色与画风审批.md)。

当整张自由生成导致眼睛、脸型、服装或方向漂移时，立即停止整图反复重绘：先锁定用户认可的正脸身份卡，再逐格生成并验收九个方向；全部单格通过后，运行 `scripts/合成角色九宫格.py` 做无生成式拼版。拼版只允许裁切、等比缩放、标签和排版，禁止再次生成脸或水平翻转。

G4 必须逐角色审批。九宫格已生成不等于已通过；所有必需角色均经用户明确确认后才能把 G4 标为 `PASS`，并进入剧情关键帧或样片。某个角色未通过时只返工该角色的新版本，不连带推翻其他已通过角色。

## 5. 管理候选与正式锚点

外部 AI 图永远先作为候选。用户文字约束高于参考图瑕疵。含纹身、饰品、同脸、风格分裂、畸形或用户禁止项的图不得晋级。

失败图移动到 `07_素材资源/候选图/第NN轮_未通过_原因/`，保留失败原因和单变量修法；不得复制到 `10_角色基准图`，不得继续生成侧面、全身或剧情关键帧。

可用脚本执行受控晋级：

```powershell
python scripts/创建九宫格清单.py --九宫格 "候选九宫格.png" --角色编号 "角色-001" --角色名 "角色名" --版本 "v1" --方向已人工复核 --禁止镜像已人工复核
python scripts/晋级角色锚点.py --项目 "项目路径" --候选 "候选九宫格.png" --清单 "候选九宫格.manifest.json" --角色目录 "角色-001_角色名" --文件名 "角色基准九宫格_v1.png"
```

逐格合成时使用：

```powershell
python scripts/合成角色九宫格.py --V01 "正面.png" --V02 "背面.png" --V03 "自身左侧.png" --V04 "自身右侧.png" --V05 "自身左前3比4.png" --V06 "自身右前3比4.png" --V07 "自身左后3比4.png" --V08 "自身右后3比4.png" --V09 "正面全身.png" --角色编号 "角色-001" --角色名 "角色名" --版本 "v1" --项目根目录 "项目路径" --输出 "项目路径/07_素材资源/候选图/第NN轮/候选九宫格.png" --方向已人工复核 --禁止镜像已人工复核
```

脚本拒绝未通过用户审批、来自失败目录、非正方形高清 PNG、方向未复核、疑似镜像或命名不合规的候选。

## 6. 编排剧本与逐 Clip 生产

依次产出项目简报、世界规则、人物关系、时间线、全季路线图、分集大纲、制作剧本、连续性表、分镜、图片提示词、视频提示词、配音字幕与交付说明。

每个 Clip 固定顺序：

```text
连续性锁定
→ 正式首帧
→ 首帧验收
→ 正式尾帧
→ 尾帧验收
→ 仅使用正式帧生成无声草稿
→ 一次只改一个变量
→ 画面锁定
→ 配音、环境声、字幕和剪辑
```

每镜只安排一个主要可见动作。参考图使用“角色名＋精确路径＋本次用途”绑定；多人镜头每名角色只给一张最匹配当前方向的身份参考，避免污染。

## 7. 通过样片门再量产

读取 [生产流水线与质检](references/生产流水线与质检.md)。样片未明确通过，不得批量生成剩余剧集。量产只能复用已验收的画风、角色、声音和镜头合同。

修改会向下游联动：角色、场景、HUD、规则或剧情变化时，检查并更新角色卡、连续性账本、剧本、分镜、提示词、静态帧记录和受影响视频清单。

## 8. 验证与交付

运行：

```powershell
python scripts/校验AIMG项目.py "项目路径"
```

脚本错误是阻塞。交付时报告已确认事实、工作假设、审批状态、文件、实际存在的素材、缺失素材、阻塞和下一步。不得宣称不存在的图、视频、声音已经生成。
