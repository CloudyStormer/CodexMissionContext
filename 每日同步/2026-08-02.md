# 2026-08-02 每日同步

## CodexMissionContext｜完全对齐交接能力

- 用户要求把两端项目列表、任务列表、任务内容、项目顺序、任务顺序的对齐方法固化并推送，使另一台设备只需发送“开始同步”即可读取和执行。
- 新增完全对齐交接包版本 `1`：当前目标为 11 个共同项目和 39 个逻辑任务；完整任务顺序按项目目标顺序分组，当前 39 项全部非置顶。该顺序是下一布局版本的确定性输入，尚未冒充有效布局版本 `3`。
- 新增运行级交接协议：A/B 分别发布任务快照，发起端生成唯一合并任务包；完整任务顺序再按两阶段协议晋升，两端镜像接收同一版八节精简语义正文，并在应用有效布局后用 SHA-256、真实 `projectId`/`threadId`、置顶和共享索引形成双端回执。
- 完整任务并集迁移允许按交接包插入 37 个新任务，但保留布局版本 `2` 既有任务的相对顺序；语义回执必须回读任务正文并重算 SHA，不能只凭发送成功。
- `check-projects` Skill、根规则、全局规则、README、项目/任务增量规则和设备状态已经接入该协议。另一端下次“开始同步”必须先读取 `同步清单/完全对齐交接包.md` 与最近未完成运行，无需用户重新解释。

## 设备 A 实际执行｜运行 `A-20260802T200017+0800`

- 设备 A 已把 AIWorkflow 快进到 `25d7ff5`，把 vid-mat-lab 快进到 `9f773f2`。统一复核的 18 个现存业务 Git 仓库均 fetch 成功、工作树干净、无冲突且 ahead/behind 为 `0/0`；本轮没有可安全新建的业务提交。
- 使用正式任务能力补建 21 个可安全创建的 B→A 镜像，含“开始”任务和其他仅 B 有实体的任务；不执行旧业务。连同既有任务，正式列表已回读目标 39 项中的 38 项，全部映射 ID 存在、非置顶，`unavailableHosts=[]`、`unavailableSources=[]`。唯一缺失是从属 Demo 的 `demo-foreman`。
- 已生成并校验 `同步运行/A-20260802T200017+0800/A-任务快照.md`：39 个逻辑 ID 各出现一次，38 项 `PRESENT`、1 项 `MISSING`，38 份八节正文 SHA-256 可重算一致。
- 本轮按规则只向设备 B 维护任务发起一次同运行请求，正式接口返回 `No Codex thread found`。A 端检查点 `ab05b97` 推送后的单次结果通知也返回同样结果，按协议不再重试。B 未发布 B 快照，A 不伪造 B 文件。

## 当前未完成项

- 设备 B 仍需把现有 `D:\.aaProject-Bruce\AIWorkFlow` 通过正式能力登记为项目，再补齐 12 个固定角色和 1 个历史停用任务镜像，并以同一运行 ID 发布 B 快照。
- Demo 前后端远程可认证，但没有任何 Git refs；A 端无法克隆真实项目或在正确项目下创建 `demo-foreman`。
- `english-talk-trainer` 和 MountainFruitCottage 公布的 `miniappframe1.0` 来源均返回 `Repository not found`。
- `funhub-WordSmiths-backend` 远端跟踪的 `.env` 含非占位敏感值；`aime-bridge-backend/.env` 和 `funhub-taro` 的三个环境文件也已被跟踪。本轮没有输出值或冒险提交；凭据轮换、停止跟踪和如需的历史清理必须由业务任务安全处理，不能在普通同步中强推。
- 因缺 B 快照，本运行不能生成唯一合并任务包，也不能发布任务完整顺序的新有效布局或 A/B 回执。当前没有正式项目任意排序/置顶能力，禁止直接写 Codex 原始状态或数据库。

因此本轮已把设备 A 可安全完成的部分做完并形成可续接快照，但不能声称 A、B 已经全部同步。
