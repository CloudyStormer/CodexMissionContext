# 2026-07-31 每日同步

## vid-mat-lab｜第 010 集拼豆图纸与水印

- 已完成：中央“小耗版权”10% 不透明度水印分享底板与第 010 集 10 秒最终成片归档。
- 成片：720×1264、30 fps、1,865,154 字节，SHA-256 `69FDA8AF90E3CC822DA00A218E8889EA85CDFBDFA65FB5A447E0B3B1E038830C`。
- 项目提交：`c9f5919 archive: complete episode 010 deliverables`，已推送 `main`，本地 HEAD 与 `origin/main` 一致。
- 保留：第 002、003、004 集已有未提交内容未纳入本次提交。
- 下一步：发布第 010 集，补作品链接并记录 24/72 小时数据。

## CodexMissionContext｜设备 A 完整同步

- 上下文：修复本机失效 SSH 别名后拉取设备 B 最新记录；并行 AI工作流任务又把部分本次规则误纳入 `cb51910`、`50c9adc` 并推送，历史保留不改写。
- 项目并集：发现设备 A 新项目 `AI工作流（试点）`，项目清单升至版本 `6`；根目录框架没有完整 Git 来源，设备 B 暂不能补齐。
- 项目登记：设备 A 已正式加入 canvas-garment、funhub-space、liquidity-bmi、liquidity-portrait、liquidity-watermark-assistant；真实 `projectId` 已写入映射。
- 项目仓库：lottery 前后端、vid-mat-lab、canvas-garment、6 个可访问 funhub 仓库和 6 个 liquidity 仓库均与 upstream 一致。
- 阻塞：两个 funhub 远端返回 `Repository not found`；Demo 两个远端没有 HEAD；Aidrama 约 1.5 GB 的普通完整 clone 仍在进行。
- 安全边界：`AI-Workflow-Dev` 在同步期间由其他任务新增本地提交 `8dd9822`（领先 `origin/main` 1 个），并保留 11 个未提交 Skill 修改；本任务未 stash、未暂存、未提交、未推送。

## AI工作流（试点）｜当天任务元数据

- 最新可见任务总数：22；`unavailableHosts=[]`、`unavailableSources=[]`。
- 本项目当天任务：活动中的“包工头”、状态为 `systemError` 的“项目经理”，以及 UI/UX设计师、产品经理、架构师、前端工程师、后端工程师、数据工程师、测试工程师、DevOps工程师、代码审查员。
- 上述 11 项均为普通本地任务，未被用户指定为双设备共同长期任务；只记录标题、项目和状态，不复制原始对话，不加入共同任务清单。

## 共享侧边栏布局｜版本 1

- 新规则：四字口令“开始同步”同时发布并对齐共享项目置顶顺序、非置顶顺序和共同长期任务置顶状态。
- 设备 A 当前依次置顶 `mission-context`、`ai-workflow`；个人项目 `AI学习` 继续永久排除。
- 另一端落后时先应用远端版本，不能用旧本地顺序覆盖；只重排共享对象子序列，个人/临时对象的置顶状态和相对顺序保持不变。
- 部分应用分别记录完整版本、目标版本和未完成逻辑 ID；能力或项目不齐时不发布残缺布局。
- Skill 通过等价 YAML 校验和两轮独立只读演练；第二轮没有剩余安全歧义。
